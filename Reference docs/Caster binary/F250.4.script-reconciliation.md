# F250.4 — Script writes and Illusion-refusal evidence

Existing-source reconciliation only. The three Warlord script families below already have source;
none needs reconstructing to identify its local write, record selector or configured hook. This
pass changes documentation only. Player-cast Illusion refusal remains an explicitly bounded
source-coverage gap, so F250.4 remains open for that claim.

## API and hook interpretation

[Scripts.TXT](../Script%20source/CAS%20reference/Scripts.TXT), sections `Base unit vs Current Unit`,
`SETSTAT`, `SETENCHANTMENTFLAG`/`SETOLENCHANTMENTFLAG`/`SETCOMBATENCHANTMENTFLAG`, establishes:
selector `B=1` means base unit, `B=0` current unit; setter family separately selects the generic,
overland or combat enchantment layer. A combat-layer flag can therefore live on the base object.
Flag setters explicitly do not trigger recalculation. `FANTASTIC(U)` reads current identity;
`BASEFANTASTIC(U)` reads base identity. `HASCOMBATGLOBAL(W,CGE,1)` asks W's own combat global.

[MODDING.INI](../Script%20source/Warlord%201.5.12.9/MODDING.INI) binds `UnitRecalculateEarly=UnitCalcPre`
and `OverlandSpellEffect=OLSpell`. [Units.RecalculateUnits.pas](Units.RecalculateUnits.pas)
`$0059A002..$0059A02C` already maps the early hook, including its configured handle and calls,
before its main enchantment processing. These are existing script semantics plus existing
compiled hook mapping, not a missing interpreter reconstruction.

## Three script families

| Family | Existing source and actual effect | Remaining calculator issue |
|---|---|---|
| Conjuring Pact “nausea” | [UnitCalcPre.CAS](../Script%20source/Warlord%201.5.12.9/UnitCalcPre.CAS), `NOCONJURERINGPACT` preceding block: in combat, read `GETCOMBATENCHANTMENTFLAG(U,EncConjuringPact,1)`. If current `FANTASTIC(U)`, set base combat `EncCreatureBinding`; otherwise subtract 10 from current `SToHit` and `SToDefend`. | No separate nausea-flag write occurs in this block. The calculator's synthetic permanent `nausea` cast and Magic Immunity gate are not verified by the known direct stat writes. Preserve `b:nausea`'s sourced arithmetic, route its input/carrier/admission discrepancy to F279.2/F279.4 and P4.11. |
| Rebuild | [OLSpell.CAS](../Script%20source/Warlord%201.5.12.9/OLSpell.CAS), `NOTREBUILD` preceding block: when `SP=SRebuild`, clear target base overland `EncRebuild`, set target base generic `EncRebuild`. `TU=SpellTargetUnit` is explicit at the script entrance. The two flag writes precede the nonhero-only stat/ability package. | Writer, destination and local order are source-present. The current single generic-flag cast omits the distinct overland clear and enclosing target/cast boundary. Keep that exact step UNVERIFIED; do not ask to reconstruct a normal overland producer already present here. P4.10 and F279.2/F279.4 handle the surrounding representation/timing. |
| Eye of Heaven | UnitCalcPre, `ENDOFCOMBAT` following block: own `HASCOMBATGLOBAL(W,CGEyeOfHeaven,1)>0` sets current generic `EncTrueSight` with selector 0. W was read from current `SOwner` at script entry. The block is reached in the combat branch, before HALT; `NOTCOMBAT` is the separate overland branch. | This is an early-recalculation grant, not a base True Sight cast. A captured Eye global used as pre-cast Illusion Immunity needs its own evidenced order/admission; the script's local grant does not establish that shortcut. F279.2/F279.4, P4.1 and P7M.16 retain the representation/boundary work. |

P4/P7M identifiers above name the existing census preparation proposals, not newly authorized work.

Rebuild's nonhero package is guarded separately: base custom attribute, Attack +2, Defense +2,
Armor Piercing, Illusion Immunity and Death Immunity. This pass does not merge that package into
the flag-write claim, nor change its existing migration ownership.

### Conjuring Pact is the identified backing spell

[MASTER.CAS](../Script%20source/Warlord%201.5.12.9/MASTER.CAS) defines `SConjuringPact=343` and
`EncConjuringPact=94`. Warlord [spells.ini](../Script%20source/Warlord%201.5.12.9/spells.ini) `[343]`
is named Conjuring Pact, group 13, EnchantmentID 94, `Custom=True`, `NonMagic=True`. Thus the
absence of a Nausea-named record in F250.1 was not absence of its backing spell. This is a
source-present classification correction, not a newly derived cast routine. Runtime loader and
complete admission remain distinct claims. In particular, NonMagic=True and the absence of a
local immunity test give no support to the calculator's automatic Magic Immunity refusal of
this synthetic input. No numeric change is made in this evidence task.

## Illusion refusal: what is actually covered

| Versions | Existing evidence | Limit |
|---|---|---|
| MoM 1.31 | [A32](../DOS%20reconstructed/A32.evidence.md) / [combat.c](../DOS%20reconstructed/combat.c), AI branch `0x80D8E..0x80DBB`, rejects a Sorcery target with Illusion Immunity. | AI target selection is not player cast admission. A32's player normal-unit Shatter gate does not settle Vertigo or Mind Storm. |
| CP 1.60 / CoM 6.08 | The same A32 address range is a Death-realm hero exclusion in these builds. Existing resistance/consumer routines remain useful for their own claims. | The 1.31 AI Illusion gate cannot be copied to these builds, and its absence there is not proof of absent player refusal elsewhere. |
| CoM2 / Warlord | [F250.1](F250.1.spell-classification.evidence.md) identifies `Illusion=True` for Vertigo and Mind Storm. [F264.1](F264.1.evidence.md) covers a portion of targeting; [F250.2](F250.2.combat.evidence.md) covers a bounded cast prefix. [Combat.ResolutionHelpers.pas](Combat.ResolutionHelpers.pas) separates resistance from Illusion attack-defense handling. | Metadata is not a player rejection branch. Neither the mapped partial target tests nor attack-defense Illusion handling establishes the two named player-cast refusals. Opaque cast helpers cannot be assumed to supply it. |

The required unanswered fact is the actual player admission predicate for Vertigo and Mind Storm
against Illusion Immunity, including the record it reads and each build's routing. No matching
complete predicate was located in the searched canonical DOS C/evidence, modern cast/targeting
reconstructions and analysis indexes. This is a search/coverage limit, not proof of engine absence.
Do not reconstruct an entire validator by default: first locate the named call path, subtract
existing coverage, then size only a necessary missing predicate under Method B. No such packet
is launched by this reconciliation. Existing known grants and resistance mechanisms need no
repeat reconstruction to answer their already-covered claims.
