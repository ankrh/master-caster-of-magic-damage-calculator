: Input - TU is the target unit's ID :
LOOP {
R=RND(33)+1;
if (R=1) THEN { SETENCHANTMENTFLAG TU,EncMithril,ABase,1; }
if (R=2) THEN { SETENCHANTMENTFLAG TU,EncAdamant,ABase,1; }
if (R=3) THEN { SETOLENCHANTMENTFLAG TU,EncOrihalcon,ABase,1; }
if (R=4) %AND (RND(2)=1) THEN { SETCOMBATENCHANTMENTFLAG TU,EncWarpResist,ABase,1; }
if (R=5) THEN { SETCOMBATENCHANTMENTFLAG TU,EncHaste,ABase,1; }
if (R=6) THEN { SETCOMBATENCHANTMENTFLAG TU,EncWaterWalking,ABase,1; }
if (R=7) THEN { SETCOMBATENCHANTMENTFLAG TU,EncLandLink,ABase,1; }
if (R=8) THEN { SETCOMBATENCHANTMENTFLAG TU,EncElementalArmor,ABase,1; }
if (R=9) THEN { SETCOMBATENCHANTMENTFLAG TU,EncIronSkin,ABase,1; }
if (R=10) THEN { SETCOMBATENCHANTMENTFLAG TU,EncRegeneration,ABase,1; }
if (R=11) THEN { SETCOMBATENCHANTMENTFLAG TU,EncResistMagic,ABase,1; }
if (R=12) THEN { SETCOMBATENCHANTMENTFLAG TU,EncGuardianWind,ABase,1; }
if (R=13) THEN { SETCOMBATENCHANTMENTFLAG TU,EncSpellLock,ABase,1; }
if (R=14) THEN { SETCOMBATENCHANTMENTFLAG TU,EncFlight,ABase,1; }
if (R=15) THEN { SETCOMBATENCHANTMENTFLAG TU,EncMagicImmunity,ABase,1; }
if (R=16) THEN { SETCOMBATENCHANTMENTFLAG TU,EncFlameBlade,ABase,1; }
if (R=17) THEN { SETOLENCHANTMENTFLAG TU,EncCCBreath,ABase,1; }
if (R=18) THEN { SETCOMBATENCHANTMENTFLAG TU,EncImmolation,ABase,1; }
if (R=19) THEN { SETCOMBATENCHANTMENTFLAG TU,EncEndurance,ABase,1; }
if (R=20) THEN { SETCOMBATENCHANTMENTFLAG TU,EncHolyArmor,ABase,1; }
if (R=21) THEN { SETCOMBATENCHANTMENTFLAG TU,EncHolyWeapon,ABase,1; }
if (R=22) THEN { SETCOMBATENCHANTMENTFLAG TU,EncHeroism,ABase,1; }
if (R=23) THEN { SETCOMBATENCHANTMENTFLAG TU,EncTrueSight,ABase,1; }
if (R=24) THEN { SETCOMBATENCHANTMENTFLAG TU,EncLionHeart,ABase,1; }
if (R=25) THEN { SETCOMBATENCHANTMENTFLAG TU,EncInvulnerability,ABase,1; }
if (R=26) THEN { SETCOMBATENCHANTMENTFLAG TU,EncCloakofFear,ABase,1; }
if (R=27) THEN { SETCOMBATENCHANTMENTFLAG TU,EncWraithForm,ABase,1; }
if (R=28) THEN { SETCOMBATENCHANTMENTFLAG TU,EncBloodLust,ABase,1; }
if (R=29) THEN { SETCOMBATENCHANTMENTFLAG TU,EncFocusMagic,ABase,1; }
if (R=30) THEN { SETCOMBATENCHANTMENTFLAG TU,EncAnimated,ABase,1; }
if (R=31) THEN { SETCOMBATENCHANTMENTFLAG TU,EncBless,ABase,1; }
} ENDLP (RND(100)>=70);

SETCOMBATENCHANTMENTFLAG TU,EncNoHeal,ABase,1;