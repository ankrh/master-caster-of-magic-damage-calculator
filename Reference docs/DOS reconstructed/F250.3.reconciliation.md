# F250.3 — DOS cast-state reconciliation

This is an inventory of existing reconstruction. The [Mind Storm/True Sight writer subunit](F250.3.mind-truesight.evidence.md) and [Warp writer subunit](F250.3.warp.evidence.md) add the bounded combat writers; their conditional binding limits apply. It covers all nine
DOS keys in MoM 1.31, CP 1.60 and CoM 6.08 independently where their sources differ. F250.3 remains
open for the direct-writer and binding questions below. No calculator number or provenance status
is changed. Modern metadata in F250.1 does not classify DOS records.

## Existing coverage and remaining claim

Addresses are executable file offsets; ranges are half-open. `131 / 160 / C1` names the three
builds. [A32](A32.evidence.md) and [combat.c](combat.c) own the existing class-13/16 prefix/store,
`[0x81F4C,0x81F6F)` and `[0x82256,0x822E9)`, in every build. On a successful resistance check it
ORs the runtime spell mask into **BATTLE_UNIT.Combat_Effects**, not `_UNITS.enchantments`.
A32 directly binds Shatter's class 16 and mask `0x10`. The other class-13 associations below are
routing leads from the retained raw SPELLDAT records, conditional on runtime loading; this pass
does not claim to have reconstructed that loader.

| Calculator key | Existing consumer in [unitcalc.c](unitcalc.c), 131 / 160 / C1 | Cast evidence and remaining work |
|---|---|---|
| `weakness` | [R6.1d](R6.1d.evidence.md): `[908D3,90942)` / `[908E2,90942)` / `[90662,906C2)`; reads Combat_Effects `0x20` | Class-13 record/mask matches A32's generic writer; exact named runtime binding remains an evidence gap. Known battle destination contradicts treating that writer as a permanent write. |
| `blackSleep` | R6.1d: no corresponding movement-clear block in 131; `[9085D,9086D)` / `[907AA,907BA)` in 160/C1; all three also consume bit `0x40` in [R6.2b](R6.2b.evidence.md) `[99EB0,99ECE)` | Class-13 record/mask matches A32. Binding remains open; additional immediate post-store effects lie beyond A32 and are not merged. The known readers do not establish those effects. |
| `shatter` | R6.1d/A32: `[90ACE,90B1C)` / same / `[907D9,90827)`; reads `0x10` | A32 already binds the named battle write and human target-kind admission. Missing citation/record implementation, not missing reconstruction of that store. AI gates are not universal human gates. |
| `vertigo` | R6.1d: `[90898,908D3)` / `[908BC,908E2)` / `[90651,90662)`; reads `0x1` | Class-13 record/mask matches A32. Named runtime binding remains open; battle write does not verify permanent placement. |
| `mindStorm` | R6.1d: `[90942,909CB)` / same / `[906C2,906F4)`; reads `0x8` | Consumer is known. The subsequent class14 subunit writes battle Combat_Effects with local raw guards. Named runtime binding remains conditional. |
| `warpAttack` | R6.1d: `[90A23,90A66)` / same / `[90749,90776)`; reads `0x80` | [Warp subunit](F250.3.warp.evidence.md) resolves spell89's helper to `AE020` and reconstructs the battle Combat_Effects store, with distinct 1.31 retry and 1.60/CoM1 cascade selection. Named binding remains conditional on runtime records/index. |
| `warpDefense` | R6.1d: `[90A66,90AAD)` / same / `[90776,90795)`; reads `0x100` | Same reconstructed helper, distinct mask. Patched final fallback may OR an already-set Defense bit. |
| `warpResist` | R6.1d: `[90AAD,90ACE)` / same / `[90795,907AA)`; reads `0x200` | Same reconstructed helper, distinct mask. No consumer rederivation needed. |
| `trueSight` | [R6.1a](R6.1a.evidence.md): `[8F338,8F359)` / same / `[8F335,8F33F)`; Specials reads enchantment `0x00400000`, grants Illusion Immunity | Specials and its supplied enchantment aggregation are known; neither identifies the direct cast's backing store. The subsequent class1/15 subunit writes battle enchantment words. Overland writer remains unlocated; named runtime binding remains conditional. |

The raw routing leads are retained in `.reviews/F250.3/packet.md`: named records 162, 165, 88,
53, 67, 89 and 131, with hashes/extraction commands. They are not a promoted reconstruction.
Search scope was the DOS reconstructed C bodies and evidence ledgers, plus the broader MoM
binary analysis index. “Not found” records this search limit; it does not prove engine absence.

## Destination and time are separate

A battle record carries both cast-state inputs and calculated fields. [D41](D41.evidence.md)
`Load_Battle_Unit` clears Combat_Effects at `0x8EAF5` in all builds. Later A32 can write that field;
R6.1d reads it while recomputing battle stats. These are separate engine operations. This
establishes the location and local ordering of the known reads/writes, not a universal recalc
after every cast, persistence after battle, or permission to place all casts before construction.

True Sight's second Specials pass receives battle enchantments absent from the permanent mask
(`(~permanent) & battle`) at 131/160 `[909CB,90A23)`, C1 `[906F4,90749)`. Its earlier constructor
pass and item source also exist in unitcalc.c. A consumer fed from several sources cannot prove
which source a direct cast wrote. F277.1 and the census's P8.1 retain their caller-schedule scope.

## What still needs evidence, and what does not

- **Citation/implementation:** reuse A32 for Shatter and the generic battle store. Keep these
  permanent-record calculator steps UNVERIFIED. F279.2 owns record design; F279.4 sizes subsequent
  migrations. No new arithmetic reconstruction is needed for any of these nine consumers.
- **Interpretation/binding:** establish named class-13 runtime records where needed; retain the
  distinction between an actual player admission path, a resistance mechanism, and AI selection.
  Full admission/recast/clear/timing remains with P3.1 (True Sight), P3.3 (battle curses), P3.4
  (Warp/mental curses); Illusion refusal stays F250.4.
- **Writer coverage update:** Mind Storm class-14 `[82451,8262D)`, True Sight combat
  class-1/15 `[8262D,8277B)`, Warp dispatch `[82D59,82E53)` plus helper `[AE020,AE126)` were retained
  preflight leads when this inventory was written. The linked Mind Storm/True Sight subunit now
  reconstructs the first two. The Warp subunit now reconstructs its resolved helper and local
  caller, with CoM1 ending at `82DAC` / `AE0BB` rather than decoding skipped/post-return bytes.
  Overland True Sight remains outside the promoted source in this reconciliation.

Warp's 1.31/1.60 caller joins existing `BU_Construct` then `BU_Apply_Battlefield_Effects` after
the resistance gate, including when its writer is skipped. CoM1 skips that entire local tail.
The [Warp recalculation assessment](F250.3.warp.evidence.md#recalculation-assessment) records
why these facts do not yet justify a full calculator pass after every effect application.

The pending class-13/16 packet extends A32 through `0x82451`. Its additional code is unnecessary
to re-establish the generic `Combat_Effects` store. Its potential value is immediate Black Sleep
side effects and post-store calls/version differences, relevant only when a downstream question
requires those facts. The unmerged derivations do not settle opaque callees or complete timing.
Retain them unchanged; do not merge them without the required successful revision/review, and do
not launch that continuation simply because it was previously prepared.
