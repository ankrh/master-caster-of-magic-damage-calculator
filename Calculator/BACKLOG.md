# Calculator backlog

Upcoming calculator work only. Every listed item is actionable unless marked **Deferred**,
**Blocked**, or as a tracking umbrella. Keep every entry concise: task, cost, dependencies, and
an evidence pointer.
Completed work and accepted decisions move to [HISTORY.md](./HISTORY.md); engine results stay in
their owning evidence documents. **`done` is not a valid backlog status:** remove a completed
item from this file and record its result in `HISTORY.md` in the same change. IDs are never reused.

Behavior is specified in [SPEC.md](./SPEC.md); working conventions are in
[CLAUDE.md](./CLAUDE.md).

Evidence short names below follow the source routing in the root [CLAUDE.md](../CLAUDE.md).
Costs are `free`, `small`, `medium`, or `large`.

Every item must carry an explicit `Versions` scope naming separately (1) each binary or build
relevant to its evidence or reconstruction and (2) each calculator version whose behavior may
change or must be verified. Write `not applicable` for either category when it has none. `all` is
acceptable for the calculator category and means `ENGINE_VERSIONS` (`steps.js`). Partial family
labels such as `DOS` or `modern` must enumerate their exact versions.

Items are implemented directly. No execution method governs them; binary reconstruction is the sole
exception and always uses method 4 from
[`DERIVATION-REVIEW-PROTOCOL.md`](../DERIVATION-REVIEW-PROTOCOL.md).

## Priority

This is the execution order; the category tables below are not ordered queues. Each numbered row
is one proposed execution package sized for one agent prompt and one review track. Multiple members
in a row are tightly related and run together under the package key shown. Every dependency must
occur in an earlier numbered row than the item it blocks. A deferred item blocks only its named
dependents, not unrelated later rows.

| # | Package | Members | Next outcome |
|---|---|---|---|
| 1 | **F192** | F192 | Read the 14 identity gates the census clears on position against their own blocks, and settle for each whether it wants the permanent record: the same question F169, F171, F172 and F179 all answered the other way. |
| 2 | **F193** | F193 | Give the census a reachability check, so a gate short-circuited in the one version whose position diverges stops reporting as an ordinary read. |
| 3 | **F163** | F163 | Remove the identity pre-pass: place each conversion at its block's position in the one sequence, so live race and Fantastic are read at position like every other field. No hoisted site is left, and the corpus now states a hero and a base race, so two of the three named prerequisites are met. **Depends on F192 and F193**, the two remaining ones: without them a 0-difference digest is still consistent with a gate wanting the permanent record or a gate its divergent version never reaches. Re-run the census before starting rather than trusting any count here. |
| 4 | **F166** | F166 | Make the building and enchantment ability grants positioned steps, so one engine block's stat write and ability write stop being split by field kind. |
| 5 | **F157** | F157 | Close the last measured version-gating leak: give Destroy Mechanical a cited version scope, which needs Q29 answered first. |
| 6 | **F168** | F168 | Gate the two Spirit Link touch-immunity reads, which make a defender immune to Dispel Evil and Exorcise in the four versions that lack the enchantment. |
| 7 | **F159** | F159 | Rule on what "same implementation in all versions" means, then scope the 43 unasserted all-version keys. |
| 8 | **F180** | F180 | Triage the five keys the new narrow-scope sweep finds live in some visible versions and silent in others, `destruction` first. |
| 9 | **F175** | F175 | Give Conjuring Pact nausea and the Wall of Fire garrison bonus the unit coverage their blocks state: neither tests for a hero, and both are gated through a predicate that excludes one. |
| 10 | **F189** | F189 | Give Eternal Night's Poor Vision penalty the one unit its block exempts by template id, which needs the special-unit vocabulary to name it. |
| 11 | **F190** | F190 | Rule on Warp Reality's Immolation To Hit arm, which the modern spell path does not support: `AttackRoll` takes the *spell table's* hitchance, not the unit's. |
| 12 | **F191** | F191 | Settle whether Rust's two independent resistance rolls may stay one control, given that the material strip and the stat penalty are separately rolled. |
| 13 | **T8** | T8 | Low priority: continue the citation triage — 163 unsourced blocks remain, now concentrated in `steps.js`, `combat_special_attacks.js` and `combat_effects.js`. |
| 14 | **F145** | F145 | Low priority: rule on version membership — 218 open-coded tests re-derive by string prefix the `SCOPE_*` sets `steps.js` already owns; the answer closes 4 of the 11 predicate groups left. |
| 15 | **F153** | F153 | Low priority: give the modern gaze rider exclusion one home; it is implemented twice in series and neither copy is ablatable alone. |
| 16 | **F146** | F146 | Low priority: give the seven roster facts the card and the matrix decode separately one reader each. |
| 17 | **F128** | F128 | Low priority: settle the picker's `Other` category, which no roster can produce. |
| 18 | **F140** | F140 | Low priority: settle the matrix row and column built for the card itself that no cell renders. |
| 19 | **F147** | F147 | Low priority: settle the six values the derivation writes that nothing reads. |
| 20 | **F129** | F129 | Low priority: give the version-scope sweep’s shared attack slot a strength, or record why not. |
| 21 | **F115** | F115 | Low priority: name the 44 Warlord spell ids the roster still renders as `Spell#N`. |
| 22 | **T15** | T15 | Low priority: fix the Markdown roster tools' drift from the roster JSON — the third `RANGED_TYPE` copy and the empty Breath/Thrown column. |
| 23 | **F131** | F131 | Low priority: re-aim the vacuous Blaze of Glory distance-penalty fixture, then settle whether a preset may state a ranged mode the engine denies. |
| 24 | **F68** | F68 | Low priority: triage the 20 MoM 1.31 presets whose positive claim no named feature moves. |
| 25 | **F69** | F69 | Low priority: the same class for CP 1.60 and CoM 6.08 (9 presets). |
| 26 | **F70** | F70 | Low priority: the same class for CoM2 and Warlord (14 presets). |
| 27 | **F71** | F71 | Low priority: triage the 33 DOS presets where one named feature is inert and another is not. |
| 28 | **F72** | F72 | Low priority: the same class for Warlord (30 presets). |
| 29 | **F73** | F73 | Low priority: the same class for CoM2 (22 presets). |
| 30 | **F74** | F74 | Low priority: confirm the 59 MoM/CP negative claims against the settled policy. |
| 31 | **F75** | F75 | Low priority: confirm the 45 Warlord/CoM2/CoM 1 negative claims, including the Land Linking pair. |
| 32 | **F76** | F76 | Low priority: triage the 37 negative claims with one live and one inert named feature. |
| 33 | **F77** | F77 | Low priority: bind the 43 presets whose key names no feature they configure. |
| 34 | **F78** | F78 | Low priority: confirm the 25 presets with nothing ablatable are baseline scenarios. |
| 35 | **F79** | F79 | Low priority: run the sweep’s `--interactions` pass and read the 26 ordering-named presets. |
| 36 | **F151** | F151 | Low priority: compute a version's default-state map without resetting and restoring the live page. |
| 37 | **F181** | F181 | Low priority: make an unrecognised shared-slot `rtbType` halt instead of deriving a unit with no attack in the slot. |
| 38 | **F182** | F182 | Low priority: settle the equivalence tool's third dead env axis, which spends a share of every version's combinations on a duplicate of the plain case. |
| 39 | **F41** | F41 | Add engine-specific hero progression as one standalone large change. |
| 40 | **M3** | M3 | After F41, implement the hero-only older-engine Destruction path. |

## Confirmed and suspected defects

| ID | Upcoming task | Cost | Evidence / dependency |
|---|---|---|---|
| F61 | **Tracking umbrella** for the preset vacuity triage; the actionable work is [F68](#confirmed-and-suspected-defects)—[F79](#confirmed-and-suspected-defects). `tools/preset_vacuity_sweep.js` runs the control [CLAUDE.md](./CLAUDE.md), *Presets* requires — remove each feature a preset configures, re-measure through `applyPreset` in the browser so the DOM and `calcKey` layers stay inside the measurement — and binds a feature to the preset’s claim by camelCase token match against the key. **Re-run 2026-08-20 after [M14](./HISTORY.md): 1010 of 1010, all five versions, every baseline reproducing its stored expectation; 345 presets carry a finding.** The class sizes the children below are cut from are the 2026-08-19 run of 987 presets, where 337 carried a finding in six disjoint classes: **A** every named feature inert, positive claim (43); **B** every named feature inert, negative claim (104); **C** some but not all named features inert, positive claim (85); **D** the same, negative claim (37); **E** the key names no feature the preset configures (43); **F** the preset configures nothing ablatable (25). Re-derive any class list with `node tools/preset_vacuity_sweep.js`. The negative-claim policy is settled and lives in *Presets*: an assertion that nothing moves is admissible only where the absence is the rule under test. Closed under this item, each with its control: `fireImmunityAfterArmorPiercing` (re-sized to breath 70 against a 200 HP defender, 20.000 against the 45 the reverse order would leave), `animatedBreathBonusCoM2` (the one cap-lift hit, 2.000 against 1.000 once the HP clip is lifted), `focusMagicDoomGazeCoM` (re-aimed to `rtbType:'gaze_multiple'` at Resistance 50, with `focusMagicDoomGazeCoM2` given the identical fixture so the pair differs only by `version:`). Two standing caveats for the children: an inert ablation says a feature moved nothing here, not why; and the `--interactions` pass has never been run over the suite ([F79](#confirmed-and-suspected-defects)). Versions: evidence/builds — not applicable; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08`, `com2_1.05.11`, `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | — | 2026-08-19 sweep run; preset contract in [CLAUDE.md](./CLAUDE.md), *Presets*; follows [F59](./HISTORY.md) and [F64](./HISTORY.md) |
| F66 | **Tracking umbrella** for the channel-conversion sequencing work; **nothing actionable remains under it** and it is ready to retire — the last item the predicate audit produced, [F93](./HISTORY.md), landed 2026-08-21 — the `flameBlade` c-pair having been merged at its settled region-`c` position ([F92](./HISTORY.md), after [F111](./HISTORY.md) took Metal Fires' own ranged write off it), the audit itself having landed ([F84](./HISTORY.md)) with its readings in the [predicate inventory](../Reference%20docs/Attack-type%20predicate%20inventory.md), the record having landed ([F88](./HISTORY.md)), both region-`d` channel writes having been positioned ([F81](./HISTORY.md), [F82](./HISTORY.md)), the pre-Focus type snapshots having been retired ([F83](./HISTORY.md)), the ungated Thrown-field writes having stopped being type predicates ([F91](./HISTORY.md), whose measurement added [F100](./HISTORY.md)) the pre-sequence type flips having been put in chain order ([F94](./HISTORY.md), whose measurement added [F101](./HISTORY.md)) the region-`e` aura ranged gates having become the strength tests the engine makes ([F96](./HISTORY.md)), the distance penalty having been aimed at the finished ranged type ([F99](./HISTORY.md)) and Focus Magic having been given the engine's four-way permanent-record branch and its three independent strength tests ([F98](./HISTORY.md)), the weapon material's strength gates having become the engine's on the modern arms ([F97](./HISTORY.md)) and then on the DOS one ([F109](./HISTORY.md)), the level ladder having been given the engine's four base-record gates and its four separate tables ([F95](./HISTORY.md), whose measurement added [F107](./HISTORY.md), which then gave the Wanderer's zero-strength ranged type a roster home and moved the Marionette retype to region `b`) and the conversions having become the field moves the engine makes, which retired the precomputed modifier pass with them ([F90](./HISTORY.md), [M14](./HISTORY.md), closing the first half of [F100](./HISTORY.md), whose ungated ranged writes then landed on their own). **Decision: no third snapshot pair — model the engine's sequential mutation instead.** Warlord has no thrown-type stat, and Blaze of Glory writes no ranged type: `UnitCalc.CAS:1490-1501` moves strength between the independent `SRanged` and `SThrown` accumulators, and nothing in `UnitCalc.CAS` writes `SRangedType,0`. No pre-sequence flip re-aims a slot's identity any more, so the `rangedTypeBeforeFocus`/`thrownTypeBeforeFocus` snapshots — which existed to undo one at the readers that noticed, each behind a per-site version ternary — corrected a problem that no longer existed and are gone with [F83](./HISTORY.md). The engine's base/current record split is **not** a snapshot and must survive: both records are live, each effect chooses per site which to read, and several write base mid-pipeline. The measured Rust defect is closed by [F81](./HISTORY.md), which also corrected Weakness, Mind Storm, Lionheart, Misfortune, Metal Fires, Wall of Fire, Reinforce Magic and Focus Magic under Blaze; Colossal Strength, Vampirism and Shadow Strike were already right. Versions: evidence/builds — Warlord 1.5.12.7 scripts, CoM2 1.05.11 `Caster.exe`; calculator — `com2_1.05.11`, `com2_warlord_1.5.12.7`. | — | Filed 2026-08-18 from the [F63](./HISTORY.md) implementation; its Giant Strength claim is **withdrawn** — `c:giantStrength`/`c:giantStrength:thrown` are `SCOPE_MOM` (`steps.js:192-193`), inert on Warlord and unable to meet Blaze. `PROVENANCE[blazeOfGlory:thrown]`; base/current record split and per-effect gates in [CoM2 binary - unit recalculation.md](../Reference%20docs/Caster%20binary/CoM2%20binary%20-%20unit%20recalculation.md); region order `stats_manifests.js`, `statChain` |
| F68 | Triage class **A** for `mom_1.31` (20 presets) — positive claims where no feature the key names moves either number. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Two clusters carry the item: the high-resistance and immunity set (`poisonHighRes`, `deathGazeHighRes`, `lifeStealHighRes`, `deathTouchHighRes`, `dispelEvilHighRes`, `stoningGazeImmunity`, `doomGazeMagicImmunity`), which the settled policy most likely admits once each `desc` says so, and the weapon/missile-immunity set (`weaponImmunityFantastic`, `weaponImmunityGeneric131`, `weaponImmunityGenericCatapult131`, `missileImmunityArmorPiercing`, `missileImmunityWIOverwrite131`, `wraithFormBypassesWIMoM`), which needs reading. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`; calculator — `mom_1.31` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F69 | Triage class **A** for `mom_cp_1.60.00` and `com_6.08` (9 presets). For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. `blessBreathBonusCoM` is one third of a cross-version triple with `blessBreathBonusCoM2` and `blessBreathBonusWarlord` in [F70](#confirmed-and-suspected-defects): one engine question, so whichever item runs first answers it for both. Versions: evidence/builds — MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`; calculator — `mom_cp_1.60.00`, `com_6.08` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F70 | Triage class **A** for `com2_1.05.11` and `com2_warlord_1.5.12.7` (14 presets). For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. The Warlord half is almost entirely the building/exclusion family (`altarOfTheSunNonHawkmanWarlord`, `alumniOfAcademyNonHalflingExcludedWarlord`, `ludusAgogeLegionaryExcludedWarlord`, `militaryWorkshopMeleeOnlyExcludedWarlord`, `lavaSmelterUpgradeRetrainHeroWarlord`), whose keys already read as exclusions and which the settled policy probably admits. Shares the `blessBreathBonus` triple with [F69](#confirmed-and-suspected-defects). Versions: evidence/builds — CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 scripts; calculator — `com2_1.05.11`, `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F71 | Triage class **C** for the DOS engines (33 presets: 27 `mom_1.31`, 3 `mom_cp_1.60.00`, 3 `com_6.08`) — a feature the key names moves nothing while another named feature does. This is the class `fireImmunityAfterArmorPiercing` belonged to, so the question per preset is whether the inert half is masked by the live one and the fixture must be re-sized, or genuinely irrelevant to the claim. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F72 | Triage class **C** for `com2_warlord_1.5.12.7` (30 presets); see [F71](#confirmed-and-suspected-defects) for what the class means. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — Warlord 1.5.12.7 scripts; calculator — `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F73 | Triage class **C** for `com2_1.05.11` (22 presets); see [F71](#confirmed-and-suspected-defects) for what the class means. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — CoM2 1.05.11 `Caster.exe`; calculator — `com2_1.05.11` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F74 | Confirm class **B** for `mom_1.31` and `mom_cp_1.60.00` (59 presets) against the settled policy: each asserts only that nothing moves, so the question is whether that absence is the rule under test. Where it is, make the `desc` say so; where the preset is named for a positive rule and merely happens to be inert, it is a class **A** defect and gets re-aimed. Reproduce the list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`; calculator — `mom_1.31`, `mom_cp_1.60.00` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F75 | Confirm class **B** for `com2_warlord_1.5.12.7`, `com2_1.05.11` and `com_6.08` (45 presets); same question as [F74](#confirmed-and-suspected-defects). Includes `landLinkingRangedCoM2`, half of the one structural finding: the version-difference subgroup `{landLinkingRangedCoM, landLinkingRangedCoM2}` expects the same 0/2 from both members, so it demonstrates nothing about versions — yet `landLinking` measures 3 against 1 on a fantastic unit’s melee, so the pair is re-aimable rather than dead. Versions: evidence/builds — Warlord 1.5.12.7 scripts, CoM2 1.05.11 `Caster.exe`, CoM 6.08 `WIZARDS.EXE`; calculator — `com2_warlord_1.5.12.7`, `com2_1.05.11`, `com_6.08` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F76 | Triage class **D** (37 presets, all five versions) — negative claims where some but not all named features are inert. The live half means the preset measures something; the question is whether the inert half is the exclusion being claimed or dead weight in the fixture. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — not applicable; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com2_1.05.11`, `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F77 | Triage class **E** (43 presets, 32 of them Warlord) — the key names no feature the preset configures, so the sweep cannot bind a claim to it and reports nothing either way. Some are token mismatches where the effect and its control have different names — the whole Goblin Pox family keys on `goblinPox` while the control is the global `poxHost` — and some are presets whose key names an effect they do not actually configure. Decide per preset between renaming the key, giving the sweep an alias map, and fixing the fixture. Reproduce the list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — not applicable; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08`, `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F78 | Triage class **F** (25 presets) — the preset configures nothing the sweep can ablate. Most are baseline and roster scenarios where that is correct and expected (`rangedMissileBasic`, `thrownBasic`, `fireBreathBasic`, the `distPenalty*` set, the `predef*` roster matchups): those need confirming, not fixing. The rest need reading for whether the scenario states any rule at all. Reproduce the list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — not applicable; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08`, `com2_1.05.11` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F79 | Run the sweep’s `--interactions` pass over the suite — it has never been run — and read the 26 ordering-named presets against it. The pass computes `both — onlyA — onlyB + neither` for each feature pair; a zero term means no ordering between the two can move the number, so an ordering-named preset carrying one is not testing the order it claims. 23 of the 26 sit inside the pass’s 2‑to‑4-feature window and 23 are clean under every other class, so this is the only check that reaches them. A zero at one operating point is a finding, not a proof of disjoint write sets: re-size the fixture before concluding. Versions: evidence/builds — not applicable; calculator — all five (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F129 | **Low priority.** The canonical version-scope sweep names the shared attack slot's type axis but leaves the slot empty, so most of that axis is still inert after [F117](./HISTORY.md) corrected the token list. `baseUnitInput` (`tools/unit_checks/assertions.js`) seeds `rtb: 0` and the sweep never overrides it, while every read that asks whether the unit *has* that attack is gated on a positive strength. **Measured 2026-08-22** over the full cross-product (11 global states x 8 unit types x 14 tokens x 5 versions): the corrected 14-token list adds **no** newly applied and no newly writing step over the old 10-token one, because at strength 0 the token only ever names an empty slot. Re-running the same cross-product at `rtb: 4` adds six applied writes, all Warlord — `b:upgradedExplosive:fireBreath`, `b:upgradedExplosive:ranged`, `base:energyCannon`, `base:militaryWorkshop`, `base:naturalSelection:wildGame`, `d:energyCannonThreshold` — and strength 0 uniquely reaches one, `b:fieryFury`, so both shapes are real: the modern roster ships a typed Ranged record at zero strength ([SPEC.md](./SPEC.md), *Attack channels on the card*). All three inventories stayed empty at both strengths, so this is missing coverage, not a known violation. What blocks it is cost, not evidence: sweeping both strengths takes `node tools/node_unit_checks.js` from ~11s to ~20s against a ~8s pre-F117 baseline, and [CLAUDE.md](./CLAUDE.md), *Tests*, uses that figure to set the cadence. Choose between sweeping both strengths across the whole cross-product, sweeping the positive strength across it plus a compact zero-strength pass, and leaving it — and update the documented runtime with whichever wins. Filed rather than folded into F117: a coverage widening has no preset that fails before and passes after, which is the adjacent-defect bound's third clause. Versions: evidence/builds — not applicable; calculator — all five, check inputs only; no number moves. | small | filed 2026-08-22 from the [F117](./HISTORY.md) implementation, which measured it |
| F157 | **Destroy Mechanical fires in the four versions whose control is hidden.** [SPEC.md](./SPEC.md), *Versions*, invariant 4. `destroyMechanicalApplies` (`combat_phases.js:592`) reads `destroyMechanical` on the attacker and `mechanical` on the defender, and neither read is scoped; the sweep cannot see it because it needs **two** hidden keys at once and `tools/hidden_control_leak_sweep.js` probes one at a time. Re-measured 2026-08-24 with a 1-figure 12 HP defender carrying only `mechanical`: melee damage goes **6 to 12** — the melee roll replaced by the deterministic kill — in all five versions, of which `mom_1.31`, `mom_cp_1.60.00`, `com_6.08` and `com2_1.05.11` are leaks and Warlord is the effect working. (The row previously said 12 to 24; that is the same doubling in a shape twice the size.) **Blocked on [Q29](#open-questions):** the three restrictions the predicate models — melee only, `atk > 0`, and firing on the counter-attack — are unsourced inferences, so no scope entry can be cited until Q29 resolves, and a gate written before then would give an invented rule a citation-shaped home. Do not guess it. No preset can fail before and pass after, since the UI clears a version-gated control; the evidence is the reproduction plus the scope-table agreement check in `tools/unit_checks/version_scope.js`. Versions: evidence/builds — Warlord 1.5.12.7 manual (the only source that states the effect); calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08` and `com2_1.05.11` change, each only for an input the UI cannot produce. | small | remainder of the three-member F157; the Blazing March gate and the Spirit Link deletion landed 2026-08-24 ([HISTORY.md](./HISTORY.md)); census [Version gating census.md](../Reference%20docs/Version%20gating%20census.md); blocked on [Q29](#open-questions) |
| F159 | **43 keys whose control exists in all five versions have no scope entry anywhere, so nothing states that their implementation is the same in each.** [F130](./HISTORY.md) measured the group: 178 read sites over 68 all-version keys, of which 20 have a `STEP_VERSION_SCOPES` entry covering all five, and 5 more were read individually and found correct with a *narrower* step scope (`animated`, `combatSummoned`, `eternalNight`, `raiseDead`, `trueSight`) because a step is not the only place an effect can be implemented. The remaining **43** are unasserted. This is not mechanical: several are cross-version in their *presence* while differing per engine in their *numbers* — `weaponImmunity`'s bonus is per engine right at `combat_special_attacks.js:526` (MoM raises to 10, CoM 1 +8, CoM2 +8, Warlord +10), and `lifeSteal`, `bless`, `blur`, `haste`, `immolation` and `fear` sit in the same position. So the ruling comes first: decide what "same implementation in all versions" has to mean before any key can be called ungatable, then apply it per key with a citation. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`, CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 scripts; calculator — all five, assertion coverage only unless a per-key reading finds a defect. | large | census [Version gating census.md](../Reference%20docs/Version%20gating%20census.md); [SPEC.md](./SPEC.md), *Versions*, invariant 4 |
| F131 | **A preset may state `rangedCheck: true` for an attacker whose derivation leaves no ranged attack, and `applyPreset` normalizes it to melee without saying so.** Measured 2026-08-22 over all 1050 presets: 243 state it and **3** resolve melee — `blazeOfGloryThrownTakesNoDistancePenaltyWarlord`, `focusMagicDoomGazeCoM` and `supremeLightSkipsZeroedRangedCoM2` — because Blaze of Glory, Focus Magic and Supreme Light each leave the Ranged field empty, and `refreshAbilityFieldVisibility` clears the box before `recalculate` reads it. In all three the `rangedCheck`/`rangedDist` fields are **inert**: removing them, or raising the distance to 20, moves neither number (8.000 / 5.000 / 0.000 unchanged), while the control `weaponMaterialDosMissileHasNoStrengthGateMoM` moves 2.000 → 5.000 when its own tick is removed. One of the three is thereby vacuous on its named axis: `blazeOfGloryThrownTakesNoDistancePenaltyWarlord` claims the distance penalty follows the attack Blaze of Glory leaves behind and states `rangedDist: 6`, but `distancePenaltyFor` (`stats.js:1712`) returns 0 on `!input.rangedCheck` before it reads the finished type, so the “−16% if the pre-sequence missile type were read” counterfactual in its `desc` cannot fire and the fixture does not discriminate its own rule; it needs re-aiming onto a case where the penalty is observable. Then decide the boundary: `applyPreset` could stop on the combination — [F125](./HISTORY.md) stops on a fixture field the version's record cannot read, and these three fields cost nothing to drop — or a fixture may legitimately state a ranged mode the engine denies, as [F118](./HISTORY.md) left version-hidden controls inert rather than halting. Versions: evidence/builds — not applicable; calculator — `com_6.08`, `com2_1.05.11` and `com2_warlord_1.5.12.7` for the three fixtures, all five for the boundary decision. | small | filed 2026-08-22 from the [F119](./HISTORY.md) measurement, which owns the numbers |
| F151 | **`getDefaultIds` builds a version’s default-state map by resetting the live page and restoring it, so every DOM observer sees a spurious full reset.** The dance (`ui_state.js`) sets `_restoring`, calls `resetCalculatorState(version)` — which ends in `recalculate()` against foreign state — snapshots the result, then `applyFullState(saved)`. Its comment claims the dance never disturbs what the user sees; it does disturb it transiently, and an observer that latches the transient keeps the wrong state after the restore. [F134](./HISTORY.md) fixed the one latch found — the tooltip controller dropped its hover owner permanently — at the observer. The structural alternative, surfaced there and not taken, is to derive a version’s defaults without touching the live DOM, from the same definitions `resetCalculatorState` reads or in a detached document, which removes the class instead of each instance. The cost is that reset path’s breadth: unit-dropdown population, per-version control visibility and default-unit selection exist only as live-DOM mutations today. Versions: evidence/builds — not applicable; calculator — all, page state only; no number moves. | medium | `ui_state.js` `getDefaultIds`, `resetCalculatorState`; found by [F134](./HISTORY.md) |
| F140 | **The matrix computes a row and a column for the card itself, then never renders them.** `buildMatrixCache` (`ui_matrix.js`) appends `selectedMatrixUnitRow('a')` and `selectedMatrixUnitRow('b')` to `attackers` and `defenders`, so the workers resolve the card's own attacker row against every defender and every attacker against the card's own defender. It then publishes `allAttackerIndexes` and `allDefenderIndexes` from `allAttackers`/`allDefenders` — the lists **before** the append — and `currentMatrixView` renders only those, so the appended row and column reach no cell. Measured 2026-08-22 with a 12-unit trimmed `mom_1.31` roster: `matrixCache.rows.length` is 13 and no `td.matrix-cell[data-atk-idx="12"]` exists. Two consequences: the extra combat resolution is discarded, and the `info.unitId != null` guard in `applyMatrixCellToMain` has an unreachable false branch — the one path that could apply a cell computed from the card as it stands. Settle which was intended: show the selected unit as its own row and column (the append already builds the labels through `selectedUnitLabel`), or drop the append and the guard branch with it. Versions: evidence/builds — not applicable; calculator — all five, matrix view only, no derived number moves. | small | filed 2026-08-22 from the [F136](./HISTORY.md) implementation, which removed the card-clobbering write that branch reached; `ui_matrix.js` `buildMatrixCache`, `currentMatrixView`, `applyMatrixCellToMain` |
| F175 | **Two Warlord region-`b` gates exclude heroes where their own blocks do not.** `b:nausea` (`stats_sequence.js`) and `wofDefenderBonusActive` (`stats.js`) both decide eligibility through `isNormalUnitType(...)`, which is false for `hero`. Neither block tests for one. Conjuring Pact's branch is `IF FANTASTIC(U)` (`UnitCalcPre.CAS:1123`) and its ELSE arm — the -10% To Hit / -10% To Defend — therefore reaches any non-Fantastic unit, hero included; the enchantment's *other* half does carry `%OR ISHERO(U)` (`EndofCombat.CAS:53`), which is what makes the silence here deliberate rather than an omission. The Wall of Fire garrison block skips only on `IF (BASEFANTASTIC(U)>0)` (`UnitCalcPre.CAS:1638`), and the helptext's "regular units" (`Unit rosters/Warlord mod unit data/HELP.TXT:2761`) is prose that does not outrank it. **Fix:** replace the predicate at these two sites with each block's own transcribed test — `SPEC.md`, *The step model*, already allows a step to name its own block's gate instead of the general rule. Filed rather than folded into [F169-F172](./HISTORY.md): those four settled which **record** each gate reads, and this is the separate question of which **units** it covers, so folding it in would have moved cases their measurement could not attribute. **Measured 2026-08-25: 64 cases.** `tools/derivation_equivalence.js` states a hero since the identity axis landed, and applying the fix above moves 64 of its 52575 derivations — 35 nausea, 30 garrison, one shared — all Warlord, all on a hero identity, and **0 in the 15525 cases the corpus held before**, which is exactly the row's earlier claim that the old list could not see this. The number is a measurement of exposure, not the evidence: it says the fix is not inert and bounds where it lands, while the claim that these two blocks admit a hero is a source reading. A preset per site is still the assertion, per [CLAUDE.md](./CLAUDE.md), *What an assertion has to be bound to*. Versions: evidence/builds — Warlord 1.5.12.7 scripts (`UnitCalcPre.CAS`); calculator — `com2_warlord_1.5.12.7`. | small | filed 2026-08-24 from the [F169-F172](./HISTORY.md) source re-reading; measured 2026-08-25 |
| F190 | **Warp Reality's second consumer, the Immolation To Hit read, has no source in the modern engine.** `toHitImmolation` (`stats.js`) starts at a fixed 0.3 and `if (warpRealityActive && !unitIsChaos) toHitImmolation = max(0.1, 0.3 - 0.2)`, i.e. it re-evaluates the Warp Reality gate against Immolation's separate spell-attack chance. But Warp Reality's block writes the **unit's** hitchance — `Dec(U.hitchance, 20)` at $005A3E33..$005A3ED0 (`Units.RecalculateUnits.pas`), `bu->tohit -= 2` at 131:0x9077A and com1:0x904DF (`unitcalc.c`) — and the modern damage-spell path never reads that field: `dam := AttackRoll(str, SpellTable[sp].hitchance) - DefenseRoll(def, Units[u].defendchance)` at $005C1696..$005C16FF (`Spells.DamageSpells.pas`) takes the **spell table's** hitchance, and the defence half is the only place a unit field enters. Immolation is spell 99 in both `spells.ini` copies. The DOS side is unread rather than contradictory: `BU_ProcessAttack` delivers Immolation through `overlay_0388_0039(SPELL_FIREBALL, …)` (131:0x99D5E, com1:0x99D6B) and that overlay is not reconstructed, so where its to-hit comes from is unknown. Decide between deleting the arm, and finding the source that supports it — and note that the calculator's own comment two lines above says Immolation is "always base 30%, ignoring all modifiers", which the arm contradicts. **Not folded into [F184](./HISTORY.md)**: F184 settled which *record* the exemption reads and its declared calculator scope is Warlord alone, where deleting the arm would move numbers in all five versions — the adjacent-defect bound's fourth clause. `warpRealityChaosExemptAtBlockImmolationWarlord` is the preset that would have to be re-aimed or dropped with it. Versions: evidence/builds — CoM2 1.05.11 `Caster.exe`, MoM 1.31 / CP 1.60 / CoM 6.08 `WIZARDS.EXE` (the unreconstructed overlay); calculator — all five. | small | filed 2026-08-25 while landing [F184](./HISTORY.md); `stats.js` `toHitImmolation` |
| F191 | **Rust's two independent resistance rolls are one control, so the material strip and the stat penalty cannot come apart.** The helptext is explicit that they are separate: a target "must resist by a -3 penalty or the unit loses Magical weapons their bonuses and permanent enchantments permanently and must resist again at -5 penalty or suffers -3 to its Melee and Physical Ranged Attack" (`Unit rosters/Warlord mod unit data/HELP.TXT:2782`), and the script splits them the same way — `COSpell.CAS:418-437` rolls the **first** one at `GETSTAT(TU,SResist,0)-3` and, on a fail, clears `EncMagic`, `EncMithril`, `EncAdamant`, `EncOrihalcon`, `EncTransmuteEquipment`, `EncResistElements`, `EncElementalArmor`, `EncFlameBlade` and `EncGuardianWind`, without ever setting `EncRust`; the recalculation block `UnitCalc.CAS:492-503` then reads `EncRust`, which only the engine's own -5 roll sets. The calculator drives both halves off one `rust` boolean (`rustActive`, `stats.js`), so the reachable state "materials stripped, no stat penalty" cannot be expressed and neither can its complement. Settle whether that is an acceptable modelling deviation — the same shape as *A landed one-time guard is projected as its persistent result* ([SPEC.md](./SPEC.md), *Deliberate deviations*) — or whether the material strip needs an input of its own. **Filed rather than folded into [F183](./HISTORY.md)**: no preset can fail before and pass after without first adding that input, which is the adjacent-defect bound's third clause. Versions: evidence/builds — Warlord 1.5.12.7 `COSpell.CAS`, `UnitCalc.CAS` and `HELP.TXT`; calculator — `com2_warlord_1.5.12.7`. | small | filed 2026-08-25 while landing [F183](./HISTORY.md); `stats.js` `rustActive` |
| F189 | **Eternal Night's Poor Vision penalty covers one unit its own block exempts.** The block's gate is four terms: `HASGLOBAL(W,GEEternalNight)=0 %AND (GetStat(U,STypeID,1)<>356) %AND (GetStat(U,SRace,0)<>RCDeath) %AND (GetEnchantmentFlag(U,EncUndead,0)=0)` (`UnitCalcPre.CAS:1340-1344`). [F186](./HISTORY.md) landed the third and fourth; the second is unimplemented, so Warlord roster template **356**, `Goblin Night Goblins` (Missile 5), takes the -2 the block exempts it from. `GetStat(U,S,1)` is the base unit (`Reference docs/Script source/CAS reference/Scripts.TXT:266`), so this is a permanent-record read of the template id and not another positional question. Filed rather than folded into F186: the calculator's single home for a template-id exception is `SPECIAL_UNIT_DEFS` (`stats_identity.js`), which F186 does not touch and which a new key extends through the identity boundary, `specialUnitForRoster`, the page's selector and its version scope — the adjacent-defect bound's first clause. Settle first whether a roster exemption with no other engine consequence earns a vocabulary entry, or whether a bare template-id read belongs at the gate. Versions: evidence/builds — Warlord 1.5.12.7 `UnitCalcPre.CAS`, `UNITS.INI`; calculator — `com2_warlord_1.5.12.7`. | small | filed 2026-08-25 while landing [F186](./HISTORY.md); `stats.js` `warlordEternalNightActive`; `Calculator/units_warlord.js` template 356 |
| F180 | **Five keys are live in some versions whose UI offers them and silent in others, and nothing says why.** The first run of the mirror-image sweep [F158](./HISTORY.md) built, `tools/narrow_control_scope_sweep.js`, 2026-08-25: over 108 keys offered in two or more versions, 435 visible (calcKey, version) pairs and 21696 exchanges, **62** keys move a number in every version whose control is visible, **36** in none (a shape gap, not a gate — the sweep's own limitation, shared with [F61](#confirmed-and-suspected-defects)), and **10** split. Five of the ten are named by a `STEP_VERSION_SCOPES` or `COMBAT_VERSION_SCOPES` entry and are settled. The **five unexplained** are `bless` (moves in both MoM, silent in CoM 1, CoM2 and Warlord), `ccFireBreath` and `supernatural` (move in CoM2 and Warlord, silent in all three DOS builds), `destruction` (same split) and `immolation` (moves in CoM 1, CoM2 and Warlord, silent in both MoM). A split is not a defect by itself — the engines may really differ — but nothing in the repo states it, which is [F159](#confirmed-and-suspected-defects)'s gap seen from the narrow side; take the ruling F159 makes and apply it here. **`destruction` first, and it is the sharpest**: its control carries `subgroup: '_'`, so the UI offers it in all five versions, while its own tooltip says "Versions: CoM 2, Warlord" and the measurement agrees. Either the subgroup is wrong — the first half of invariant 4, a control shown where the engine has no effect — or DOS Destruction is missing. Filed rather than folded into F158: none of the five is in code that item touched, each needs its own source reading, and a UI-visibility correction has no preset that fails before and passes after, which is the adjacent-defect bound's first and third clauses. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`, CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 scripts; calculator — all five, unknown until each key is read. | medium | filed 2026-08-25 from the [F158](./HISTORY.md) implementation, which built the sweep; census [Version gating census.md](../Reference%20docs/Version%20gating%20census.md); [SPEC.md](./SPEC.md), *Versions*, invariant 4 |
| F181 | **`deriveUnitStats` accepts an unrecognised `rtbType` silently instead of halting.** Measured while landing [F161](./HISTORY.md): a shared-slot token outside `RANGED_TYPES`, `THROWN_TYPES` and `GAZE_TYPES` (`data.js`) is read as three separate `'none'` results by `buildSlotContext` (`stats.js`), so the unit silently derives with no attack in the slot at all rather than the run stopping. That is the shape `SPEC.md`, *Out-of-range values stop the run* forbids, and it is what let F161's `'stoning_gaze'` typo pass a negative assertion vacuously for as long as it did — the same latent no-op class [F117](./HISTORY.md) corrected elsewhere. **Not folded into F161** because the correction is in `stats.js`, which that item does not touch. Halting needs a decision on the tokens that legitimately mean "no attack" (`'none'`, and the modern record whose `rtbType` names nothing because it has no shared slot), so scope it before implementing. Versions: evidence/builds — not applicable; calculator — all five, input validation. | small | measured 2026-08-25 while landing [F161](./HISTORY.md); `Calculator/stats.js` `buildSlotContext`, `Calculator/data.js` |
| F182 | **`derivation_equivalence.js` carries a third dead axis: its `chaos-channels` env sets an input the calculator does not have.** The env is `{ chaosChannels: 'demonWings' }` (`tools/derivation_equivalence.js`, `ENVS`), but `input.chaosChannels` is read nowhere in `Calculator/` — measured 2026-08-25 by an exhaustive counted grep of the directory: every one of the 47 occurrences in `Calculator/*.js` is a `chaosChannels:*` step id, a `PROVENANCE` label or a helper's name, and none is a read of the input — and the three real controls are the `ccDefense`, `ccFlight` and `ccFireBreath` booleans of `ENCHANTMENT_DEFS`. So the env produces an ordinary unit and exercises no axis, exactly like the two [F113](./HISTORY.md) and [F117](./HISTORY.md) already corrected in the same list. It is not merely inert: an env is drawn per combination case, so a dead one silently spends a share of the 3900 combinations per version on a duplicate of `plain`. The fix is to decide whether an env may carry abilities at all — today the combination pass overwrites `over.abilities`, so it cannot — and then either drop the entry or give the pass a way to merge one. **Not folded into [F178](./HISTORY.md)**, which added the `chaos-surge` env beside it: the adjacent-defect bound's third clause wants a preset that fails before and passes after, and this tool is a diagnostic outside `npm test`, so no preset can bind it. Versions: evidence/builds — not applicable; calculator — measurement tooling only, no version behavior. | small | measured 2026-08-25 while landing [F178](./HISTORY.md); `tools/derivation_equivalence.js` `ENVS` |
| F192 | **The 14 identity reads the census clears on position have never been read against their own blocks.** `tools/identity_read_position_census.js` reports which record a site is *handed*, never which record its block *reads*: a `late-ok` verdict says only that every identity conversion precedes the site's owning chain entry, so the fixed point and the record at that entry are the same value. Whether the block wanted the **permanent** record instead is a separate question the tool does not ask, and the four corrections that asked it — [F169](./HISTORY.md), [F171](./HISTORY.md), [F172](./HISTORY.md), [F179](./HISTORY.md) — all answered it the other way. The 14: `step:leadershipAura`, `step:metalFires` and `step:soulLinkerAura` (`combat_abilities.js`); `step:soulLinkerAura` (`stats_sequence.js`); and `darknessAtkDefMagnitude`, `darknessResBonus`, `eternalNightEnemyResPenalty`, `metalFiresActive`, `misleadEligible`, `nodeAuraActive`, the two `realmWardActive` reads, `spellWardActive` and `supremeLightEligibleAt` (`stats.js`). **The exposure is measured, not hypothetical**: on the widened corpus the census reports the permanent and calculated records differing in up to 1985 cases at one of these sites (up to 473 before the identity axis), so a wrong choice is worth real numbers. **Scope:** read each block in the source its `PROVENANCE` names and record which record its own test uses, exactly as F169/F171/F172/F179 did; correct the ones that disagree, each with a preset. **Blocks [F163](#modelling-work)**, whose scope is "give each fixed-point read the record its own block reads" — that cannot be discharged without this reading, and F163's 0-difference bar is blind to it by construction, since a late-ok read keeps its value when the pre-pass is removed. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`, CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 `Caster.exe` and scripts; calculator — all five. | medium | filed 2026-08-25 from the census's own printed limits; `tools/identity_read_position_census.js`, `BLIND_SPOTS` |
| F193 | **Nothing checks reachability behind the census's hoisting verdict.** `tools/identity_read_position_census.js` decides hoisting from chain rank alone: a site owned by entry K is hoisted for a field when some conversion of rank at or after K writes it. It never asks whether the gate is *evaluated* in the version whose position diverges, so a read short-circuited by a version test in exactly that version reports as an ordinary hoisted or late read. `c:shatter` was the one site carrying a marker for this; [F188](./HISTORY.md) found the marker was answering the wrong question — the read is a targeting restriction — and the marker went with the reclassification, leaving the class unflagged. **Scope:** enumerate the identity-reading gates whose expression carries a version disjunct or a version-scoped `if`, and for each state whether the gate is reachable in every version the oracle ranks it in; give the tool a declared marker where it is not, so a future case is found rather than reasoned about. Small because the population is the gates already tabulated in `SITES` and the test is a grep of each expression for a version term. **Blocks [F163](#modelling-work)**: without it a 0-difference digest is consistent with a gate whose divergent version never runs it. Versions: evidence/builds — not applicable; calculator — all five, measurement tooling and gate expressions only. | small | filed 2026-08-25 from the census's own printed limits; `tools/identity_read_position_census.js`, `BLIND_SPOTS` |
| F168 | **Spirit Link makes a defender immune to Dispel Evil and Exorcise in the four versions whose control is hidden.** [SPEC.md](./SPEC.md), *Versions*, invariant 4. `dispelEvilFailProb` (`combat_special_attacks.js:88`) and `exorciseFailProb` (`:113`) each open with `if (hasAbil(defAbilities, 'spiritLink')) return 0;` and no version test, so the hidden key turns a resist-or-banish touch attack off entirely. Measured 2026-08-24 with a 1-figure 12 HP `fantastic_death` defender at Resistance 3: damage 12 → 6 and destroy 1 → 0 in `mom_1.31`, `mom_cp_1.60.00`, `com_6.08` and `com2_1.05.11`; Exorcise against `fantastic_nature` moves the same. Warlord is the effect working and must not change. Spirit Link is Warlord's alone (`PROVENANCE[spiritLink] versions=com2_warlord_1.5.12.7`, `stats_identity.js`), which is the citation for the gate; unlike the `dosDefenseForAttack` term [F157](./HISTORY.md) deleted, these two sites are reachable in every version, so they need a real gate rather than deletion. Decide at the same time whether the two share one predicate — both encode the same fact, that Spirit Link strips fantastic targeting — and whether it belongs with the `d:spiritLink` step that already clears `fantastic`. **The census's leak enumeration is incomplete, and this is the evidence:** its 32-site table lists only `combat_effects.js:904` for this key, because no sweep shape gives the attacker a touch attack against a Fantastic defender — the same blind spot that hid the eight. **Both functions now carry an anchor** — `PROVENANCE[dispelEvilTouchRider]` `SCOPE_MOM` and `PROVENANCE[exorciseTouchRider]` `SCOPE_COM_PLUS` ([F158](./HISTORY.md)) — so the gate this row needs is a *defender-side* one and must not be confused with the rider's own scope: the version set here is Spirit Link's, not the rider's. Versions: evidence/builds — Warlord 1.5.12.7 scripts; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08` and `com2_1.05.11` change, each only for an input the UI cannot produce. | small | filed 2026-08-24 from the [F157](./HISTORY.md) implementation, which measured it; census [Version gating census.md](../Reference%20docs/Version%20gating%20census.md) |
| F41 | Add engine-specific hero progression: the DOS eight-threshold ladders and template-ability formulas (including CoM's Blademaster divisors, mana table and Lucky rewrite), plus the modern nine-step hero table. Decide how the current six-entry level control exposes them. | large | R6.1f evidence, hero ladders and abilities; CoM2 tables, level bonuses; `combat_abilities.js:43-78` |

## Modelling work

Accepted limitations remain in `SPEC.md`; only planned changes appear here.

| ID | Upcoming task |
|---|---|
| F163 | **Remove the identity pre-pass.** No engine has an identity phase: the realm write sits inside the same `if` block as that block's stat writes, at that block's address — Chaos Channels' demon-skin armor does `Inc(U.defense, 3)` and `U.race := 18; U.Fantastic := True` in one block at `$0059F4A3` (`Units.RecalculateUnits.pas`), and `unitcalc.c` addresses each DOS realm write at its own `BU_Apply_Specials` offset. `applyOrderedIdentityConversions` (`stats_identity.js`) instead resolves live race and Fantastic to a fixed point before any stat write. The cause is the calculator's own shape, not the evidence: `deriveUnitStats` computes its gates as constants ahead of the sequence, and the pre-pass is what makes those constants valid — which leaves `race` and `fantastic` the only fields exempt from the step model's rule that a step reads a field's current value at its own position. **Scope:** give each fixed-point read the record its own block reads; put each conversion at its block's chain position; then the six `:race` step ids collapse into their stat siblings ([SPEC.md](./SPEC.md), *An id is the effect a player selects*) and `DEDUCED_IDENTITY_C_POSITIONS` (`stats_manifests.js`) shrinks to `c:raiseDead` alone — that one entry stays inherited for the reason the DOS list already records, being a combat-spell write with no block of this routine to order it against, so the list does not empty. **The loadout and experience eligibility gates come with it**, because they are the same hoist with the same patch: the engine tests `if B.Fantastic then U.level := 1` at $0059A118 and `not B.Fantastic and not B.ishero` inside the weapon block at $0059E2B8, where `loadoutEligible` and `levelEligible` (`stats.js`) are constants computed once before the sequence. **`EncDestiny` is what makes both hoists need an answer rather than a mechanical move:** it writes the *base* record `B.race`/`B.Fantastic` at $0059A390, mid-routine, and the two level gates immediately above it read `B.Fantastic` before that write ($0059A118, $0059A172) while the weapon block below reads it after. The calculator patches around this with a separate `destinyActive` term at both consumers; a positional conversion must either keep that term or give Destiny a real `base`-phase write of the permanent record, which is what lets both hoists dissolve. **Its positional prerequisite is cleared: `tools/identity_read_position_census.js` reports no hoisted site.** Re-measured 2026-08-25 after [F187](./HISTORY.md) and [F188](./HISTORY.md), and again after the corpus widened. The tool claims every read of the calculated identity in the `data-scope="core"` sources (an unclassified read halts it), assigns each the chain entry owning the effect it gates, and decides hoisting exhaustively over the *whole* conversion set instead of ablating one: a read at entry K is hoisted for a field exactly when some conversion of rank at or after K writes that field, which covers live **race** as well as Fantastic. Of its **70** sites, 35 declare or forward a channel, 5 are post-chain combat reads of the finished record, **2 are cast-time targeting restrictions** (Rust, [F183](./HISTORY.md); Shatter, [F188](./HISTORY.md)), 14 are positional replays already correct — the tool also checks that no conversion lies between a replay's key and its block, and none does — 14 are fixed-point reads whose owning entry every conversion precedes, and **none is hoisted**. It re-finds all sixteen landed corrections — [F167](./HISTORY.md), [F169-F174](./HISTORY.md), [F176-F179](./HISTORY.md), [F184-F187](./HISTORY.md) — independently of how each was found, and throws if it ever stops doing so. **So no read is hoisted; the item is still blocked, on evidence rather than on position — see F192 and F193 below.** **What its 0-difference bar is worth now.** The bar is a full-digest comparison at 0 differences over `tools/derivation_equivalence.js`, and that corpus was weaker than the number looked — three rounds in a row read the same evidence as readiness and were wrong. One of the three named limits is closed. **(1) Closed 2026-08-25:** the case list now states a permanent identity. Three identities cross the whole list — the raceless custom unit it always had, a High Men non-hero and a High Men hero — and eighteen more (six race-gated building races, a Fantastic record, a Fantastic hero, four special units, six templates, one hero type) are drawn in one extra combination block per version; 52575 derivations, ~26s, from 15525 and ~10s. Re-measured against it: F187's Tactician site moves **34** cases where it moved 0 (13 CoM 1, 15 CoM2, 6 Warlord), [F175](#confirmed-and-suspected-defects) moves **64** where it moved 0, F178 moves 108 where it moved 24, and F185/F186 move 36 and 4 where they moved 8 and 1. Widening it also fired the census's own incompleteness guard on a conversion the oracle had never discovered — `b:fieryFury:race`, invisible because its write matched the base race the only Fantastic axis stated — so the conversion set the hoisting verdict is computed over was itself incomplete until this round. It changed no verdict: still 70 sites, none hoisted. **(2) and (3) still stand, and each is now filed:** [F192](#confirmed-and-suspected-defects) reads the 14 late-but-correct sites' blocks, since "no conversion follows this entry" answers position but not which record the block wants — the question F169, F171, F172 and F179 all answered the other way, and the widened corpus shows the two records are distinguishable in up to 1985 cases per site, from up to 473 before; [F193](#confirmed-and-suspected-defects) is the reachability check over gates whose expression carries a version disjunct, unflagged since F188 retired the one marker. Both must precede this row. Two further limits are not prerequisites but bound the claim: each site's **owner chain entry is judgement**, defended only by its citation, and two rows re-read this tranche overturned theirs outright (F183, F188); **write-side omissions are out of scope** — [F176](./HISTORY.md) was a conversion the sequence did not make at all — and **post-chain reads are assumed correct by construction**. So today a 0 over the widened corpus is a real claim about heroes, base races and the identity conversions, and still consistent with a late-ok read that wants the permanent record or a gate unreachable in the version whose position diverges. And which *units* a gate covers remains a different question from which *record* it reads, which is why [F175](#confirmed-and-suspected-defects) is adjacent rather than a member. One caveat for that evidence: the combination pass indexes `ENVS` and `SHAPES` through the seeded generator, so lengthening either list reshuffles every combination case and per-case counts recorded before the change are not comparable with counts after it; the identity axis is exempt because the generator is reseeded per identity, and the 15525 pre-existing cases are byte-identical across the widening. Recorded as a deviation in [SPEC.md](./SPEC.md), *Deliberate deviations*. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`, CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 `Caster.exe`; calculator — all five, structure only. Cost: large. |
| F166 | **Make the building and enchantment ability grants positioned steps.** Seven nested calls in `deriveUnitStats` fold ability grants into the ability set before the sequence runs — `applyLavaSmelterGrant`, `applySanctaBasilicaGrant`, `applyDivineProtectionGrant`, `applyPillarOfFaithGrant`, `applyInsulationGrant`, `applyFortificationGrant`, `applyOutlanderReformGrants` (`stats_identity.js`), plus `deriveMarionettePackage`. **The split is by field kind, not by evidence.** The `CreateUnit.CAS` grants already have chain phases for their stat halves — `base:sanctaBasilica` carries the +3 Resistance while the Sanctify/Lucky/Magic Immunity grants from the *same four mutually-exclusive `STypeID` branches* sit outside the chain; the same holds for `base:pillarOfFaith` and the Lava Smelter pairs. Worse, Divine Protection (`UnitCalcPre.CAS:874-882`), Insulation (`UnitCalcPre.CAS`) and Fortification (`UnitCalcPre.CAS` and `UnitCalc.CAS`) cite regions **b** and **d**, which the chain already walks, so these are positioned engine writes hoisted out of it. The stated reason is calculator plumbing, written down at `stats_identity.js`: "Merging up-front — rather than into effectiveAbilities — lets the Flame Blade grant reach the weapon-upgrade and stat-bonus logic, which read the raw ability set." **Scope:** give each grant a step at its cited phase writing the ability set, beside the stat step already cited to the same block; the `effectiveAbilities` reads that motivated the hoist become positioned reads. Larger than [F163](#modelling-work) — the ability set is consumed by hundreds of constants computed before the sequence — and it should follow it, since both are the same class and F163 settles how a hoisted constant becomes a positioned read. Note the ordering constraint the current shape hides: the grants must precede `base:immunityCurseGating` ([F164](./HISTORY.md)), because Sancta Basilica confers the Magic Immunity that step reads. **Expected to move no number**, so the evidence is a full-digest comparison at 0 differences (`tools/derivation_equivalence.js`). Recorded as a deviation in `SPEC.md`, *Deliberate deviations*. Versions: evidence/builds — CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 scripts; calculator — `com2_1.05.11` and `com2_warlord_1.5.12.7`, structure only. Cost: large. |
| M3 | **Blocked by F41:** implement the older-engine Destruction path, including elemental protection. Present in all three DOS builds but hero-only, so it stays unreachable until heroes are modelled; 1.31 adds the defender's per-figure hits where CoM 1 adds a flat 100. |

## Context cost and maintainability

T8, T15, F115, F128 and F145–F147 are documentation- and naming-quality items filed here
for want of a better home. F145–F147 are the three drift classes [T13](./HISTORY.md) measured
non-empty; `tools/drift_class_sweep.js` is the measurement and re-running it is the first step of
each.

| ID | Upcoming task | Cost |
|---|---|---|
| T8 | **Low priority.** Give the code's unsourced engine claims either a citation or an evidence home. **[T6](./HISTORY.md)'s counts are withdrawn:** T6 committed no script, so its block definition is unrecoverable and its figures are not reproducible by any definition tried. `tools/comment_citation_census.js` fixes a definition and is the measurement; re-run it as the first step. `tools/comment_quotation_check.js` is the companion check on quotations — run it on each source triaged, and read every `MISSING` it reports, since it cannot tell an attributed quotation from the author's own scare-quote. Eleven stand repository-wide today: `steps.js` 4, `combat_abilities.js` 1, `combat_effects.js` 1, `combat_phases.js` 1 (the [Q29](#open-questions) Destroy Mechanical quotation), `stats_manifests.js` 1, `stats_sequence.js` 1, `stats_identity.js` 1 (correct usage — it quotes the block's own withdrawn wording) and `combat.js` 1, that last one triaged 2026-08-25 and benign: it is the calculator's own breakdown-row label `"Hasted 2nd Strike + Counter"`, not an attributed quotation. It reads its twelve formula-bearing sources from `provenance_audit.js`'s `calculatorFiles` plus `steps.js` and `stats_manifests.js`, and splits each multi-line prose block three ways: **anchored** (a `PROVENANCE`/`STAT-FORMULA` line within eight lines, so `npm run provenance` checks it), **pointer** (the prose names where the claim lives), and **unsourced**. **All earlier counts are superseded by [T16](./HISTORY.md)**, which taught the pointer test to read an executable address and a `Unit rosters/` path. **Repository total after the 2026-08-25 round: 648 multi-line blocks, 214 anchored, 271 pointer, 163 unsourced (57.0 KB).** Re-measure before each round; the earlier rounds in that session moved the figure well below what this row last recorded. **Rounds so far.** 2026-08-22: `combat_fear_and_touch.js`, `combat_state.js`, `combat_phases.js`, `combat.js` and `stats_manifests.js`; one category-3 claim became [Q29](#open-questions). 2026-08-23: `stats_identity.js` (19 unsourced to 10) and `steps.js` (34 to 33); one code defect filed as [Q30](#open-questions), and two quotations called fabricated — **one of which was not**, see below. That round also withdrew the Magic Immunity block's "overwhelming effective resistance" explanation as unsourced; **that withdrawal was itself wrong and is reversed** — see the 2026-08-24 fourth round. **2026-08-24, third round: `stats.js`, in full.** Every one of its 109 blocks read rather than sampled; after, **7 anchored / 131 pointer / 27 unsourced (6.9 KB)**, and the repository total **633 / 207 / 245 / 181 unsourced (59.4 KB)**. **The (a) split inverts here, and that is the round's main result.** Only **27 of 109 document the calculator's own machinery** — 25%, against `steps.js`'s 77% — because `stats.js` is the effect-gate file: 82 of its blocks stated an engine fact and every one now carries a citation or an anchor pointer. So the headline count's looseness is per-file, not global, and cannot be extrapolated from one source to the next. **Three findings.** (1) **The 2026-08-23 round's first "fabricated quotation" was not fabricated, and the finding is withdrawn.** `"If the friendly unit already has Large Shield ability, the unit receives Missile Immunity bonus instead"`, which the Fortification block attributed to helptext, is verbatim at `Unit rosters/Warlord mod unit data/HELP.TXT:1678`; that round searched `Reference docs/` alone and the version-matched helptext lives under `Unit rosters/`. The comment now carries that line again. **The second stands, re-verified rather than inherited:** `"Cannot be blocked by … Magic Immunity"` has no occurrence under either root, and could not have — it is written with an elision. Its claim is sound and now better sourced than the manual changelog that replaced it: Hierophany's spell record carries `NonMagic=True` (`spells.ini` [239]), which that file's legend defines as not blocked by Magic Immunity. The method is now `tools/comment_quotation_check.js`. (2) The gaze-existence block's example was **version-wrong**: Gorgons ship `Ranged 0` in CoM 6.08 but `Ranged 1` in both MoM builds, so the comment now names the build. The rule it illustrates, and the code, were right. (3) The race gate on the Warlord race-exclusive buildings is **the calculator's, not the script's**: `CreateUnit.CAS` tests only `ISBUILT(C,B<building>)`, and race decides which city may hold the building. Recorded in the comment; no defect, since the calculator has no city model. **2026-08-24, fourth round: the Magic Immunity curse block (`stats_identity.js`), triggered by [F164](./HISTORY.md).** Two corrections and one narrowing. **(1) The 2026-08-23 round's withdrawal of the "overwhelming effective resistance" mechanism is reversed — the mechanism is real and is now the block's `PROVENANCE[immunityCurseGating]`.** Modern `GetEffectiveResistance` sets Resistance to 100 for a Magic-Immune target of any realm spell (`$00595BEF`, `Combat.ResolutionHelpers.pas`) and DOS adds 30 (131:0x990AE, `combat.c`), each against a roll that cannot reach it, so the curse never lands. That round searched the Fandom page rather than the reconstructions and concluded absence from it. **(2) `ACCurse`/`ACGlobalEffect` are AI weights, not a resolution mechanism** (`spells.ini` lines 244-256, "strategic (off-screen) combat results"), and disagree with the engine flag on Mind Storm and Temporal Twist — recorded in the block so a later round does not reach for them. **(3)** Both halves of the membership are now sourced — inclusions from `spells.ini`'s `NonMagic` legend, exclusions because Black Prayer and Eternal Night's Darkness are combat globals no unit-flag roll ever consults — **so the block leaves T8 entirely.** **2026-08-25, fifth round: `combat.js`, in full.** All 34 multi-line blocks read; **21 unsourced to 4 (5.1 KB to 0.9 KB)**, the four remaining being genuine calculator machinery (the file header, the phase-closure preamble, the inert Dispel Evil compatibility path and the self-convolution note). `combat.js` is the pipeline file, so its blocks resolve overwhelmingly to *pointers* — it states almost no formula of its own and still holds 0 anchors — which is a third file shape again, after `stats.js`'s effect gates and `steps.js`'s machinery. **Three findings.** (1) **A wrong claim, corrected: the Cause Fear block located Magic Immunity's resistance bonus "in `fearFailProb`", and it is not there.** `fearFailProb` (`combat_fear_and_touch.js`) tests Death Immunity and the CoM `-3` only; Magic Immunity is never named in it. The bonus arrives already folded into `aResDeath`/`bResDeath` by the resistance transform — `+30` in DOS (`PROVENANCE[dosEffectiveResistance:magicImmunity]`), an assignment to 100 in the modern builds (`PROVENANCE[effectiveResistance:magicImmunity]`) — so the comment was wrong twice, on location and on the modern value. Comment-only defect: the code is right, and the split it now cites is the standing rule in `SPEC.md`, *Combat resolution contract*. (2) **The Black Sleep early return is half engine and half calculator, and the comment claimed all of it as engine.** A Black-Sleeping attacker landing no blow is `ApplyAttack`'s `figs := 0` at `$005B19C4`; suppressing the *incoming* Wall of Fire, retaliation and counterattack is the calculator's own boundary, no build having such a gate. Now labelled as such. (3) **`Reference docs/Caster binary/CoM2 binary - combat flow.md` had gone stale against the calculator** — it registered F29, F30 and F31 as live discrepancies, all three of which have since landed. Corrected to past tense pointing at `HISTORY.md`, per the rule that evidence documents record findings and never mirror live state. The same three sentences also carried double-encoded mojibake where `→` belonged (`c3a2 e280a0 e28099`); fixed in passing. No anchor hashes that file, so the audit was unaffected. **No new defect and no new open question: the two corrections were both comment-side, with the code right underneath.**

**What remains**, re-measured 2026-08-25, is 182 unsourced blocks: `steps.js` 31 (of which 31 were measured as code documentation in the 2026-08-23 round — effectively closed), `combat.js` 23, `combat_special_attacks.js` 17, `combat_phases.js` 14, `combat_effects.js` 14, `combat_abilities.js` 13, `combat_state.js` 12, `stats_identity.js` 11, `combat_fear_and_touch.js` 9, `stats_sequence.js` 9, `stats.js` 27 (all code documentation) and `stats_manifests.js` 2. The next round's scope is `combat.js` and `combat_special_attacks.js`, the two largest untriaged populations. Two things a future round must know. **(a)** The count is an upper bound and the margin is per-file: reading is the only thing that separates a block documenting the code from an unsupported engine claim, and the ratio has now measured 77% code-doc in `steps.js` and 25% in `stats.js`. **(b)** A pointer closes the evidence-home half but not the audit half: `npm run provenance` still cannot see a claim without an anchor beside it. Whether these claims should instead get real `STAT-FORMULA`/`PROVENANCE` anchors — declaring formula ids for combat-resolution routines that are not stat transforms, extending `directFunctionIds`, and getting each span reviewed — is a larger, separate decision that this row does not make. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`, CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 `Caster.exe`; calculator — all five, comments only, no behavior change. | large |
| T15 | **Low priority.** The two Markdown roster tools read tokens and fields the roster JSONs do not carry. `RANGED_TYPE` (`tools/generate_mom_com2_unit_comparison.py:13`) is the third copy of the map [T12](./HISTORY.md) deleted, keyed `missile`/`rock`/`magic_c`/`magic_n`/`magic_s`; every roster JSON carries the display spelling instead — the modern pair `Missile`, `Boulder`, `Magic`, `Magic-lightning` from `tools/ranged_types.py`, the DOS pair those plus `Magic(C)`/`(N)`/`(S)` and the three `Gaze(...)` from `parse_tweaker_unit_data.py` — so no key matches and the tool's Ranged column falls to its `"?"` default. Drop it as T12 did. Separately, `breath_str` in **both** tools reads `breath` and `breath_type`, keys no roster JSON has: the records carry `fire_breath`, `lightning_breath`, `thrown` and `thrown_breath`/`thrown_breath_type`, so the Breath/Thrown column reads `-` for all 194 CoM2 units, hiding 10 fire-breath, 2 lightning-breath, 8 thrown and 20 thrown-breath attackers, and `BREATH_TYPE` is dead with it. Deciding which of those four fields the one column shows is the work. Filed rather than folded into T12: the comparison tool is outside the file T12 touched and no preset can hold either fix, failing the adjacent-defect bound's first and third clauses. `MoM CoM2 unit comparison.md` is not checked in, so only `Unit rosters/CoM2 unit roster.md` regenerates. Versions: evidence/builds — not applicable; calculator — none, roster documents only, no number moves. | free |
| F145 | **Low priority. Half one is done; half two is a decision the user must rule on, below.** **Re-measured 2026-08-25**, `node tools/drift_class_sweep.js predicates`: **21 groups over 612 compound tests in 33 sources**. Two premises in the old row were wrong and are corrected here: the 22 was measured over **609** tests, not 611, and only **one** group has closed since — [F132](./HISTORY.md)'s has-ranged-attack predicate. [F162](./HISTORY.md)'s roster-to-special-unit map is a *maps* group and was never in the predicates class. **Ten groups closed 2026-08-25 (21 → 11); see [HISTORY.md](./HISTORY.md).** **The 11 that remain.** Four are version-family tests the decision below collapses. Two are compound combat facts that merely contain a version local (`!isCoM1Only \|\| woundedTopFigHP(…) <= 24`, `combat_state.js`; `sAlive <= 0 \|\| cap <= 0 \|\| (isCoM2 && aBlackSleep)`, `combat_phases.js`) and stay as they are. Three are the counter-attack and immolation expressions duplicated between the haste and no-haste First Strike paths — a restructure of that block, not a predicate merge. One is `activeIndex >= 0 && items[activeIndex]`, identical in two peer comboboxes (`ui_units.js`, `ui_matrix_properties.js`), where neither file owns the shared `.unit-dropdown-item` contract, so the home is genuinely unsettled. The last is the base-Fantastic decode, and **it is three shapes, not two**: `combat_effects.js` twice with a `(unitType \|\| '').startsWith('fantastic_')` fallback and `combat_abilities.js:985` without it, so the copies disagree about a unit with neither `identity.baseFantastic` nor `abilities.baseFantastic` set. Unifying them needs a cited source for which reading is the engine's, so it is not a merge; it is a defect awaiting evidence. **DECISION — does version membership get a named home?** Re-measured 2026-08-25: **218 open-coded tests across 17 sources** — 155 `startsWith` on a version-family prefix and 63 `===`/`!==` against an exact id — not the old row's 201. Six families cover all 155: `com2_warlord`/`com2_warlord_` 53, `com2`/`com2_` 62, `com` 16, `com_` 10, `mom`/`mom_` 13, `mom_cp_` 1. The same fact is also re-derived as a local **22 times** under five different names (`isCoM2`, `isModern`, `isWarlord`, `isCoM`, `isCoM1`), with `com2` and `com2_` used interchangeably for one set. **The row's premise that version membership "has no named home at all" is false.** `steps.js` already owns the vocabulary as exact-member sets — `SCOPE_DOS`, `SCOPE_MOM`, `SCOPE_COM1`, `SCOPE_COM_PLUS`, `SCOPE_MODERN`, `SCOPE_WARLORD` — with the comment *"Every set names its exact members: family labels alone are not a scope"*, and two membership readers over them, `stepAppliesToVersion` and `combatEffectInVersion`, both of which **halt** on a key with no scope, citing `SPEC.md`, *Out-of-range values stop the run*. Each of the six prefix families maps exactly onto one of those sets. So the 218 sites are not an unnamed fact; they are a **deviation from a vocabulary this repo already treats as authoritative for step and effect scoping**, and the prefix form is the part `steps.js` explicitly rules out. **Option A — the rewrite.** Add family predicates over the existing `SCOPE_*` sets in `steps.js` (`data-scope="core" data-worker`, so **no new bundle cost and no new scope tag**; every page source loads after it) and route the sites through them. Collapses ~199 of the 218 sites; the ~19 genuinely single-version tests keep their meaning through the one-member scopes `SCOPE_COM1`/`SCOPE_WARLORD`/`SCOPE_MOM_1_31`. Closes **4 of the 11 remaining groups (11 → 7)** and deletes all 22 locals. Equivalence is exhaustively checkable — the domain is five ids, so a probe can prove every rewritten site answers identically for all five. **It is not behavior-neutral in one respect, and that is the point:** `version.startsWith('com2')` answers a silent `false` for a misspelled or out-of-range version and the run continues with a DOS-shaped answer, where a set-membership predicate can halt, which is what the repo's own rule requires. The user must rule on whether to take that halt. Cost: **medium** — 218 mechanical one-line substitutions in 17 files, uniform and machine-checked, plus judgment on ~19 single-version sites and on the halt. **Option B — the local fix.** Close the 4 version groups with four file-local helpers. Cost: small. But it closes the same 4 groups while leaving ~211 sites open-coded and **adds four more file-local names for facts `steps.js` already names**, so it improves the sweep's number and makes the underlying drift slightly worse. **Recommendation: Option A, with the halt**, as its own round. Versions: evidence/builds — not applicable; calculator — all five; no number moves in either option, but Option A changes out-of-range version handling from silent to halting. | medium |
| F153 | **The modern gaze rider exclusion is implemented twice in series, and ablating either copy alone moves nothing.** `gazeTouchParams` (`combat_phases.js`) zeroes the six rider probabilities under `modernGazeSkipsRiders`, and `buildAttackerGazePhase` / `buildDefenderGazePhase` separately hardcode `poisonStr`, `poisonFail`, `stoningFail`, `deathTouchFail`, `exorciseFail` and `destructionFail` to 0 and `lifeStealMod` to null in their `isCoM2` `commonSpec`. Measured 2026-08-23 during [F152](./HISTORY.md): setting `modernGazeSkipsRiders = false` moved no number, feeding the raw gaze rider probabilities into `commonSpec` moved none either, and only both together did. The `isCoM2` branch consumes just `aDispelEvilWithGaze`, so the other seven `*WithGaze` values `gazeTouchParams` computes are read only by the DOS branch. Give the rule one home — the phase builder is the site that binds — so an edit to the other copy cannot look effective while doing nothing. Versions: evidence/builds — not applicable; calculator — `com2_1.05.11` and `com2_warlord_1.5.12.7`; no number moves. | small | measured during [F152](./HISTORY.md); `combat_phases.js` `gazeTouchParams`, `buildAttackerGazePhase`, `buildDefenderGazePhase` |
| F146 | **Low priority.** Seven roster facts are decoded independently by the card and the matrix: `hp`, `melee`, `defense`, `resist`, `to_hit`, `category` and `name`, each read by both `setRosterUnitRecords` (`ui_units.js`) and `buildMatrixUnitStats` (`ui_matrix.js`). That is [F121](./HISTORY.md)'s shape — two readers of one record that can disagree — and F121 gave only the modern attack record a single reader. **Measured 2026-08-22** with `node tools/drift_class_sweep.js readers`: 14 of the 23 roster fields the sources read have more than one reader, from a 29-field record. The count is a floor: only a receiver that is a parameter of its function is attributed, so a decode through a local (`const unit = units.find(…)`) is invisible, which is how `spec_att_attrib` (read once, at `ui_units.js:580`) and the `unit.race` column drop out. **The argument-record axis is clean and needs no work:** the three `deriveUnitStats` call sites state 43 keys, and the only 4 that diverge are `hitMelee`/`hitRanged`/`hitThrown`/`hitBreath`, omitted by `buildMatrixUnitStats` where the roster has no per-channel source and `parseInt(…) || 0` supplies the same 0 the card writes. Versions: evidence/builds — not applicable; calculator — CoM2 and Warlord for the modern channels, all five for the shared numeric fields. | small |
| F147 | **Low priority.** Six values the derivation writes are read by nothing. **Measured 2026-08-22** with `node tools/drift_class_sweep.js writeonly` over 51 names written onto the abilities map, counting occurrences across `Calculator/`, `tools/` and `tests/` with comments blanked — a comment is not a reader. `liveRace` (`stats.js:1134`) is written beside `liveFantastic`, which 13 sites read. The other five are the Marionette strayed-branch grants (`stats_identity.js:495-501`) — `transmuteEquipment`, `sage`, `mechanicalMaster`, `ritualMaster`, `arcaneWard` — written in the same object literal as `rebuild` (36 reads), `charmed` (24) and `spellLock` (17). **`transmuteEquipment` is not a missing effect:** Transmute Equipment is implemented as the gated step `b:marionette:strayedTransmute` (`stats_sequence.js:382`, `steps.js:154`), so the flag is a redundant second home, and the question per flag is whether it is that, a record kept for a consumer not yet written, or dead. `package.grantedAbilities` publishes the same eight grants as display labels and nothing reads that either. **One adjacent member, from the same run's `enums` mode:** `REALM_PANEL_CLASSES` (`ui.js:206`) names `panel-realm-normal`, the one of its seven with no rule in `Calculator/style.css`. Versions: evidence/builds — not applicable; calculator — Warlord for the Marionette grants, all five for `liveRace` and the panel class. | small |
| F128 | **Low priority.** `populateUnitDropdown`'s `categoryOrder` (`ui_units.js`) names an `Other` bucket no roster can fill. **Measured 2026-08-22** over all four rosters: the categories emitted are the 18 race names, `Generic`, `Special` and the six realms, and neither generator family can produce `Other` — the modern pair route `Race=` through their race and realm tables and raise on an id in neither (`tools/generate_com2_units_json.py`, `tools/generate_warlord_units_json.py`), while the DOS parser copies the source's race column verbatim (`tools/parse_tweaker_unit_data.py:324`), where no row spells it. Harmless while an unnamed bucket was skipped; since [F116](./HISTORY.md) the loop throws on one, so the list is an enumeration and a member nothing can satisfy is a claim without a referent. Either delete the entry or record what would produce it. Filed rather than folded into F116: no source cites the entry and no preset can hold its removal, which fails the adjacent-defect bound's second and third clauses. Versions: evidence/builds — not applicable; calculator — all five, dead-entry removal, no unit enters or leaves any picker. | free |
| F115 | **Low priority.** 44 of the 50 spell ids the Warlord roster uses have no display name, so 95 `Spellcaster=` entries in `units_warlord.js` read `Spell#260x3`. `SPELL_NAMES` in both roster generators lists six ids (Web, Fireball, Doom Bolt, Healing, Raise Dead, Summon Demon); a miss renders the record's own id verbatim, which invents nothing and is why [F113](./HISTORY.md) left it rather than converting it. Nothing in `Calculator/` reads the value — only `tools/generate_com2_unit_roster.py` displays it — so this is roster-document quality, not a calculation defect. `MASTER.CAS` is the numeric ID map (`S<Name> = <id>`) and `DESC.INI` carries the display spellings; transcribe both and reconcile them, since the six existing entries use display names rather than the `MASTER.CAS` identifiers. Regeneration must move only these strings. Versions: evidence/builds — Warlord 1.5.12.7 `MASTER.CAS` and `DESC.INI`, CoM2 1.05.11 the same; calculator — `com2_warlord_1.5.12.7` and `com2_1.05.11` roster metadata only, no number moves. | small |

## Open questions

| ID | Question / next action | Evidence / blocker |
|---|---|---|
| Q1 | Resolve Troll Shaman/Magician roster values versus the manual, including the possible Troll +1 melee rule. | roster/manual conflict |
| Q2 | Confirm the apparent Draconian common-unit +1 Resistance racial modifier. | roster/manual conflict |
| Q5 | **Blocked:** identify the source and page behind “weird defense behavior on page 25.” | Missing source |
| Q6 | Resolve CoM High Prayer's +3-attack text versus the +2 used elsewhere. | prose conflict |
| Q8 | Determine whether Wraiths use Life Steal −4 or −3. | observation/manual conflict |
| Q12 | Determine when “ranged” includes missile, boulder, magical, Thrown, breath, or gaze across versions/effects. | Asked; awaiting answer |
| Q15 | Check whether compiled code ever consumes `unitT.savemodifier`. | CoM2 units, `unitT` fields; R4 |
| Q18 | Determine what CoM 1's unit-type ceiling of `0x97` selects, given that the helptext says "normal unit" but the ceiling appears to admit four fantastic summons. Two blocks read it: the Holy Arms grant (`com1:0x8F16C`) and Heavenly Light's weapon-quality test (`com1:0x905E7`), where it forces `cl` to Magic Weapons and so withholds the To Hit thresholds. [F49](./HISTORY.md) implements the weapon-quality condition and leaves the ceiling unmodelled pending this answer. | R6.1a evidence, *Open questions*; R6.1c evidence, *The CoM 1 city-defense bytes are Guardian and Heavenly Light* |
| Q19 | Resolve CoM 1 Realm Ward helptext's −2 To Hit/−4 Defense/−4 Resistance against the executed `0x90A87..0x90A9B` writes of −2/−3/−3. | R6.1d evidence, *CoM 1 Realm Wards use −2/−3/−3* |
| Q20 | Identify the CoM 1 `Move_Flags 0x0100` bit set by Supreme Light and verify whether its read path implements the helptext's post-combat regeneration. | R6.1d evidence, *CoM 1 Supreme Light has five eligibility paths* |
| Q21 | Resolve CoM 1 helptext's retained Endurance/Giant Strength powers against the manual's Teleportation/Inner Fire/Divine Protection replacements; the executed binary effects match the manual's replacement package. | prose conflict; R6.1e evidence, *Later builds turn the item block into a patch surface* |
| Q23 | Resolve CoM Logistics scaling: shipped helptext says +0.5 movement per two experience levels, while the manual says +0.5 per level. `Battle_Unit_Moves2` consumes the per-controller maximum at `DS:0x3AC8` but does not calculate it. | R6.1g evidence, *CoM `Battle_Unit_Moves2`*; prose conflict |
| Q25 | Resolve the R6.1h shared-source rendering disagreement at `0x8E5C6: 25 00 01`. Claude: the statement-level `#if` is “a naming branch only” and may use `IP_COM1_DIVINE_PROTECTION` versus `IP_POWER_DRAIN`. Codex: a statement-level `#if` “visually asserts a source/control-flow divergence” absent from the identical bytes, so one statement plus a build-selected alias is preferable. The merged C uses the alias form and the mechanic is undisputed. | R6.1h evidence, *Disputed source representation* |
| Q26 | Resolve the R9-G1a-R3 evidence-scope disagreement. Codex: “Retain only raw `[0x2AED2,0x2AED4)` (`81 00`) for the target value” and cite checked-in mapping/name evidence. Claude: “Restricting the read to the two target bytes is exactly the position that produced the wrong record base”, so the target's neighbouring name-pointer rows, table origin, ability-mask bounds and selected strings are the minimum locate evidence. The base, target address/value and constructor linkage are undisputed. | R9-G1a-R3 evidence, *Disputed evidence scope*; data D1–D8 |
| Q28 | Decide what realm a Fantastic unit with a mundane base race has. `legacyUnitTypeFromIdentity` and `legacyUnitTypeFromLiveIdentity` (`stats_identity.js:71`, `:109`) map the six realm races plus `No Heal` to a realm token and read everything else as `arcane`. No shipped roster reaches it — every `baseFantastic` record in all four rosters carries a realm race — but the UI does: the Fantastic checkbox and the Base race select are independent, so Fantastic + Barbarian is a legal control combination that silently becomes `fantastic_arcane`. `UNITS.INI` cannot express the pair (a fantastic record has `Race >= 15`), so the sources leave it undefined and [F113](./HISTORY.md) filed it rather than converting a live UI path into a crash. Either source the answer, restrict the control pair, or record the `arcane` reading in `SPEC.md` as a deliberate deviation. | sources silent; filed 2026-08-21 from the [F113](./HISTORY.md) sweep |
| Q30 | **Code defect, not only a comment defect.** Sancta Basilica (Warlord) grants Sanctify to Crusaders and Paladins in `applySanctaBasilicaGrant` (`stats_identity.js`), and the source says it should not. `Reference docs/Script source/Warlord 1.5.12.7/CreateUnit.CAS` lines 412-441 writes the per-type grants in four *mutually exclusive* `STypeID` branches, each closed by `GOTO "ENDOFUNIQUEBUILDING"`: 108 (High Men Monks) and 231 (High Men Inquisitors) get Sanctify plus improved Exorcise; 111 (Crusaders) gets Lucky alone; 113 (Paladins) gets Magic Immunity alone. The +3 Resistance ahead of the branches does apply to every unit trained there, and is separately sourced on `PROVENANCE[sanctaBasilica]`. `DisAbil.CAS` lines 1036-1046 groups all three types under one "Sainthood" display line and is the likely origin of the conflation, but it is display-only. Sanctify drives the Life-realm unit-type conversion, so the fix moves numbers: it needs a preset that fails before and passes after, plus a full `npm test`. | source read and confirmed; filed 2026-08-23 from the [T8](#context-cost-and-maintainability) triage |
| Q29 | Settle Destroy Mechanical's gates (Warlord, Clockwork Tinmen). The only source that states the effect is the Warlord manual — the summon "could destroy any mechanical unit in 1 hit with its special ability" — and an exhaustive search of `Reference docs/` (66 occurrences of `Destroy Mechanical`/`Clockwork`, every one read) finds no Warlord script and no `Caster.exe` reconstruction that names it; `DESC.INI[340]` describes a different spell. `destroyMechanicalApplies` (`combat_phases.js`) restricts it to melee, requires `atk > 0`, and fires on the counter-attack, and none of the three is sourced. Same class as [F141](./HISTORY.md), which asked the same question of the modern touch-rider gate. | sources silent; filed 2026-08-22 from the [T8](#context-cost-and-maintainability) triage |
| Q31 | Settle what `@Units@IsChaosUnit` tests, because the calculator answers it two different ways for two blocks that call it. Chaos Surge ($005A1271) reads it as `unitRealm === 'chaos'` (`realmOfUnitType`, live race, no Fantastic term) and Blazing Eyes ($005A1E16) as `unitType === 'fantastic_chaos'` (`blazingEyesActive`, `stats.js`). The helper is external to `Units.RecalculateUnits.pas` and is not reconstructed, so which reading is right is unread rather than decided; the only stated fact is that it takes the *calculated* record (`CoM2 binary - unit recalculation.md`, Spell Ward: "A unit converted to Death or Chaos earlier in region `c` is therefore warded"). The two readings can only disagree on a unit whose live race is Chaos while its live Fantastic is false, which no conversion in `stats_identity.js` produces — every Chaos write sets both — **but Spirit Link produces exactly that state**: it clears Fantastic at `d:spiritLink` and touches no race, so a spirit-linked Chaos creature is `normal_chaos` at the fixed point. The disagreement is therefore reachable, and [F184](./HISTORY.md) added a third call site to the question. **Two facts found 2026-08-25 while landing F184.** (a) `IsChaosUnit` is called by Warp Reality's block too — `(not IsChaosUnit(i))` at $005A3E33..$005A3ED0 — and the DOS block that same one transcribes tests the **realm alone**: `bu->race != rt_Chaos` at 131:0x9077A and com1:0x904DF (`unitcalc.c`). That is the only address-backed statement of the predicate anywhere, and it agrees with the Chaos Surge reading, not the Blazing Eyes one. (b) Against that, the modern routine reaches for the helper exactly where a plain race test would not do: both the Spell Ward chain and `@Map@NodeAuraType` decide Nature and Sorcery by `race` equality and Chaos by `IsChaosUnit` (`CoM2 binary - unit recalculation.md`), which is weak evidence that the helper is *not* `race = 18`. A fourth site reads it a fourth way — `nodeAuraActive` (`stats.js`) uses `unitRealm`. So the ruling now covers four calculator sites and must be made from the helper, not by analogy. Not folded into F184: with the helper unreconstructed there is no citation to back either reading, which is the adjacent-defect bound's second clause, and a race-only rewrite would move numbers in all five versions where F184's declared scope is Warlord. | filed 2026-08-24 from the [F174](./HISTORY.md) block reading; extended 2026-08-25 from [F184](./HISTORY.md); `combat_abilities.js` (`realmOfUnitType`), `stats.js` (`blazingEyesActive`, `unitIsChaos`, `nodeAuraActive`) |

## Blocked on people

| ID | Needed answer |
|---|---|
| H1 | **Human:** Contact Seravy about the confirmed CoM2/Warlord Blur bug: turn-relative `CGADEnemy` makes the opposing army's Blur reduce that army's own retaliation gazes and counterattack. Record whether this is intended and whether a future engine build should change it. |
