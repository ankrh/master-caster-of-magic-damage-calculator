# Calculator backlog

Upcoming calculator work only. Every listed item is actionable unless marked **Deferred**,
**Blocked**, or as a tracking umbrella. Keep every entry concise: task, cost, dependencies, and
an evidence pointer.
Completed work and accepted decisions move to [HISTORY.md](./HISTORY.md); engine results stay in
their owning evidence documents. **`done` is not a valid backlog status:** remove a completed
item from this file and record its result in `HISTORY.md` in the same change. IDs are never reused.

Behavior is specified in [SPEC.md](./SPEC.md); working conventions are in
[CLAUDE.md](./CLAUDE.md).

Evidence short names below follow the source routing in the root [CLAUDE.md](../CLAUDE.md).
Costs are `free`, `small`, `medium`, or `large`.

Every item must carry an explicit `Versions` scope naming separately (1) each binary or build
relevant to its evidence or reconstruction and (2) each calculator version whose behavior may
change or must be verified. Write `not applicable` for either category when it has none. `all` is
acceptable for the calculator category and means `ENGINE_VERSIONS` (`steps.js`). Partial family
labels such as `DOS` or `modern` must enumerate their exact versions.

Items are implemented directly. No execution method governs them; binary reconstruction is the sole
exception and always uses method 4 from
[`DERIVATION-REVIEW-PROTOCOL.md`](../DERIVATION-REVIEW-PROTOCOL.md).

## Priority

This is the execution order; the category tables below are not ordered queues. Each numbered row
is one proposed execution package sized for one agent prompt and one review track. Multiple members
in a row are tightly related and run together under the package key shown. Every dependency must
occur in an earlier numbered row than the item it blocks. A deferred item blocks only its named
dependents, not unrelated later rows.

| # | Package | Members | Next outcome |
|---|---|---|---|
| 1 | **F113** | F113 | Bring the codebase under the fail-loud rule: convert every invented fallback into a stop. |
| 2 | **F109** | F109 | Decide whether the DOS material body's fabricated input-strength gate comes off. |
| 3 | **F92** | F111, F92 | Take Metal Fires' ranged write off the Flame Blade step, then merge the c-pair; the region-`b` question is already settled. |
| 4 | **F107** | F107 | Give the Wanderer a roster ranged type and move the Marionette retype off the permanent record. |
| 5 | **F93** | F93 | Decide whether the magical-ranged flag is loaded from `RangedType.INI` or stays six fixed names. |
| 6 | **F108** | F108 | Settle CoM 1 Focus Magic's ammunition gate and the breath/gaze reach of its two `+3` arms. |
| 7 | **D28** | D28 | Bound the CP-only Wall of Fire trigger without widening D22. |
| 8 | **D37** | D37 | Reconstruct the bounded CoM 6.08 routine containing the Supernatural consumer. |
| 9 | **F41** | F41 | Add engine-specific hero progression as one standalone large change. |
| 10 | **M3** | M3 | After F41, implement the hero-only older-engine Destruction path. |
| 11 | **F60** | F60 | Decide whether the DOS per-figure Doom Gaze scaling should be modelled. |
| 12 | **F106** | F106 | Decide whether MoM 1.31's second `BU_Apply_Specials` call needs a chain position. |
| 13 | **T8** | T8 | Low priority: give the unsourced engine claims in the code a citation or an evidence home. |
| 14 | **T9** | T9 | Low priority: rename the `legacy*` DOS identifiers; no behavior change. |
| 15 | **T10** | T10 | Low priority: retire the three To-Hit seed steps’ split and the two leftover field qualifiers. |
| 16 | **T11** | T11 | Low priority: drop the Chaos Surge carve-out the corrected fire-breath position made inert. |
| 17 | **F68** | F68 | Low priority: triage the 20 MoM 1.31 presets whose positive claim no named feature moves. |
| 18 | **F69** | F69 | Low priority: the same class for CP 1.60 and CoM 6.08 (9 presets). |
| 19 | **F70** | F70 | Low priority: the same class for CoM2 and Warlord (14 presets). |
| 20 | **F71** | F71 | Low priority: triage the 33 DOS presets where one named feature is inert and another is not. |
| 21 | **F72** | F72 | Low priority: the same class for Warlord (30 presets). |
| 22 | **F73** | F73 | Low priority: the same class for CoM2 (22 presets). |
| 23 | **F74** | F74 | Low priority: confirm the 59 MoM/CP negative claims against the settled policy. |
| 24 | **F75** | F75 | Low priority: confirm the 45 Warlord/CoM2/CoM 1 negative claims, including the Land Linking pair. |
| 25 | **F76** | F76 | Low priority: triage the 37 negative claims with one live and one inert named feature. |
| 26 | **F77** | F77 | Low priority: bind the 43 presets whose key names no feature they configure. |
| 27 | **F78** | F78 | Low priority: confirm the 25 presets with nothing ablatable are baseline scenarios. |
| 28 | **F79** | F79 | Low priority: run the sweep’s `--interactions` pass and read the 26 ordering-named presets. |

## Evidence-source prerequisites

| ID | Required source artifact | Cost | Method / evidence |
|---|---|---|---|
| D28 | Reconstruct the MoM 1.31/CP 1.60 routine `[0x8A90D,0x8B30D)` and the version-specific helper at `0x89448` (1.31 `[0x89448,0x89505)`; CP `[0x89448,0x89454)`) to identify the CP-only `0x8AF6B -> 0x89448 -> Check_Wall_Of_Fire_Attack` trigger and determine calculator relevance. Write the source-shaped body to `Reference docs/DOS reconstructed/combat.c` and durable evidence to `Reference docs/DOS reconstructed/D28.evidence.md`. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`; calculator — `mom_1.31`, `mom_cp_1.60.00`. | large | **Method 4:** [D22 evidence](../Reference%20docs/DOS%20reconstructed/D22.evidence.md) proves the extra CP incoming edge but keeps the containing trigger semantics outside its frozen table extent |
| D37 | Reconstruct the complete CoM 6.08 far routine `[0x9BCE0,0x9D535)` in `Reference docs/DOS reconstructed/combat.c`. Account for internal BP-sharing near helpers ending at `0x9BE1F`, `0x9C082`, `0x9C0FB`, and `0x9C121`; all semantic branches, calls, tables, state reads/writes, and exits; overlay `$03D8` slot-0 callers `0x6C3C4` and `0x7FC98`; and the adjacent slot-1 boundary at `0x9D535`. Bind the argument, `DS:[0x9F20 + arg0*0x04C8]`, candidate filters, complete state transition, and the `$2000` Supernatural bypass without assuming a post-combat or player-record identity. Versions: evidence/builds — CoM 6.08 `WIZARDS.EXE`; calculator — not applicable. | large | **Method 4 fixed extent:** [D34 evidence](../Reference%20docs/DOS%20reconstructed/D34.evidence.md) proves the direct Supernatural gate, exact natural boundary, two callers, sole `retf`, and no intervening overlay entry; SHA-256 `f7f19608db94c4f058916e831610147792ebde0b5cff7d4086ea151413cfc75f` |

## Confirmed and suspected defects

| ID | Upcoming task | Cost | Evidence / dependency |
|---|---|---|---|
| F61 | **Tracking umbrella** for the preset vacuity triage; the actionable work is [F68](#confirmed-and-suspected-defects)—[F79](#confirmed-and-suspected-defects). `tools/preset_vacuity_sweep.js` runs the control [CLAUDE.md](./CLAUDE.md), *Presets* requires — remove each feature a preset configures, re-measure through `applyPreset` in the browser so the DOM and `calcKey` layers stay inside the measurement — and binds a feature to the preset’s claim by camelCase token match against the key. **Re-run 2026-08-20 after [M14](./HISTORY.md): 1010 of 1010, all five versions, every baseline reproducing its stored expectation; 345 presets carry a finding.** The class sizes the children below are cut from are the 2026-08-19 run of 987 presets, where 337 carried a finding in six disjoint classes: **A** every named feature inert, positive claim (43); **B** every named feature inert, negative claim (104); **C** some but not all named features inert, positive claim (85); **D** the same, negative claim (37); **E** the key names no feature the preset configures (43); **F** the preset configures nothing ablatable (25). Re-derive any class list with `node tools/preset_vacuity_sweep.js`. The negative-claim policy is settled and lives in *Presets*: an assertion that nothing moves is admissible only where the absence is the rule under test. Closed under this item, each with its control: `fireImmunityAfterArmorPiercing` (re-sized to breath 70 against a 200 HP defender, 20.000 against the 45 the reverse order would leave), `animatedBreathBonusCoM2` (the one cap-lift hit, 2.000 against 1.000 once the HP clip is lifted), `focusMagicDoomGazeCoM` (re-aimed to `rtbType:'gaze_multiple'` at Resistance 50, with `focusMagicDoomGazeCoM2` given the identical fixture so the pair differs only by `version:`). Two standing caveats for the children: an inert ablation says a feature moved nothing here, not why; and the `--interactions` pass has never been run over the suite ([F79](#confirmed-and-suspected-defects)). Versions: evidence/builds — not applicable; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08`, `com2_1.05.11`, `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | — | 2026-08-19 sweep run; preset contract in [CLAUDE.md](./CLAUDE.md), *Presets*; follows [F59](./HISTORY.md) and [F64](./HISTORY.md) |
| F66 | **Tracking umbrella** for the channel-conversion sequencing work; the actionable work left is the two items the predicate audit produced or confirmed — [F92](#confirmed-and-suspected-defects) and [F93](#confirmed-and-suspected-defects) — the audit itself having landed ([F84](./HISTORY.md)) with its readings in the [predicate inventory](../Reference%20docs/Attack-type%20predicate%20inventory.md), the record having landed ([F88](./HISTORY.md)), both region-`d` channel writes having been positioned ([F81](./HISTORY.md), [F82](./HISTORY.md)), the pre-Focus type snapshots having been retired ([F83](./HISTORY.md)), the ungated Thrown-field writes having stopped being type predicates ([F91](./HISTORY.md), whose measurement added [F100](./HISTORY.md)) the pre-sequence type flips having been put in chain order ([F94](./HISTORY.md), whose measurement added [F101](./HISTORY.md)) the region-`e` aura ranged gates having become the strength tests the engine makes ([F96](./HISTORY.md)), the distance penalty having been aimed at the finished ranged type ([F99](./HISTORY.md)) and Focus Magic having been given the engine's four-way permanent-record branch and its three independent strength tests ([F98](./HISTORY.md)), the weapon material's two modern strength gates having become the engine's ([F97](./HISTORY.md), which added [F109](#confirmed-and-suspected-defects)), the level ladder having been given the engine's four base-record gates and its four separate tables ([F95](./HISTORY.md), which added [F107](#confirmed-and-suspected-defects)) and the conversions having become the field moves the engine makes, which retired the precomputed modifier pass with them ([F90](./HISTORY.md), [M14](./HISTORY.md), closing the first half of [F100](./HISTORY.md), whose ungated ranged writes then landed on their own). **Decision: no third snapshot pair — model the engine's sequential mutation instead.** Warlord has no thrown-type stat, and Blaze of Glory writes no ranged type: `UnitCalc.CAS:1490-1501` moves strength between the independent `SRanged` and `SThrown` accumulators, and nothing in `UnitCalc.CAS` writes `SRangedType,0`. No pre-sequence flip re-aims a slot's identity any more, so the `rangedTypeBeforeFocus`/`thrownTypeBeforeFocus` snapshots — which existed to undo one at the readers that noticed, each behind a per-site version ternary — corrected a problem that no longer existed and are gone with [F83](./HISTORY.md). The engine's base/current record split is **not** a snapshot and must survive: both records are live, each effect chooses per site which to read, and several write base mid-pipeline. The measured Rust defect is closed by [F81](./HISTORY.md), which also corrected Weakness, Mind Storm, Lionheart, Misfortune, Metal Fires, Wall of Fire, Reinforce Magic and Focus Magic under Blaze; Colossal Strength, Vampirism and Shadow Strike were already right. Versions: evidence/builds — Warlord 1.5.12.7 scripts, CoM2 1.05.11 `Caster.exe`; calculator — `com2_1.05.11`, `com2_warlord_1.5.12.7`. | — | Filed 2026-08-18 from the [F63](./HISTORY.md) implementation; its Giant Strength claim is **withdrawn** — `c:giantStrength`/`c:giantStrength:thrown` are `SCOPE_MOM` (`steps.js:192-193`), inert on Warlord and unable to meet Blaze. `PROVENANCE[blazeOfGlory:thrown]`; base/current record split and per-effect gates in [CoM2 binary - unit recalculation.md](../Reference%20docs/Caster%20binary/CoM2%20binary%20-%20unit%20recalculation.md); region order `stats_manifests.js`, `statChain` |
| F68 | Triage class **A** for `mom_1.31` (20 presets) — positive claims where no feature the key names moves either number. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Two clusters carry the item: the high-resistance and immunity set (`poisonHighRes`, `deathGazeHighRes`, `lifeStealHighRes`, `deathTouchHighRes`, `dispelEvilHighRes`, `stoningGazeImmunity`, `doomGazeMagicImmunity`), which the settled policy most likely admits once each `desc` says so, and the weapon/missile-immunity set (`weaponImmunityFantastic`, `weaponImmunityGeneric131`, `weaponImmunityGenericCatapult131`, `missileImmunityArmorPiercing`, `missileImmunityWIOverwrite131`, `wraithFormBypassesWIMoM`), which needs reading. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`; calculator — `mom_1.31` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F69 | Triage class **A** for `mom_cp_1.60.00` and `com_6.08` (9 presets). For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. `blessBreathBonusCoM` is one third of a cross-version triple with `blessBreathBonusCoM2` and `blessBreathBonusWarlord` in [F70](#confirmed-and-suspected-defects): one engine question, so whichever item runs first answers it for both. Versions: evidence/builds — MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`; calculator — `mom_cp_1.60.00`, `com_6.08` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F70 | Triage class **A** for `com2_1.05.11` and `com2_warlord_1.5.12.7` (14 presets). For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. The Warlord half is almost entirely the building/exclusion family (`altarOfTheSunNonHawkmanWarlord`, `alumniOfAcademyNonHalflingExcludedWarlord`, `ludusAgogeLegionaryExcludedWarlord`, `militaryWorkshopMeleeOnlyExcludedWarlord`, `lavaSmelterUpgradeRetrainHeroWarlord`), whose keys already read as exclusions and which the settled policy probably admits. Shares the `blessBreathBonus` triple with [F69](#confirmed-and-suspected-defects). Versions: evidence/builds — CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 scripts; calculator — `com2_1.05.11`, `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F71 | Triage class **C** for the DOS engines (33 presets: 27 `mom_1.31`, 3 `mom_cp_1.60.00`, 3 `com_6.08`) — a feature the key names moves nothing while another named feature does. This is the class `fireImmunityAfterArmorPiercing` belonged to, so the question per preset is whether the inert half is masked by the live one and the fixture must be re-sized, or genuinely irrelevant to the claim. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F72 | Triage class **C** for `com2_warlord_1.5.12.7` (30 presets); see [F71](#confirmed-and-suspected-defects) for what the class means. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — Warlord 1.5.12.7 scripts; calculator — `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F73 | Triage class **C** for `com2_1.05.11` (22 presets); see [F71](#confirmed-and-suspected-defects) for what the class means. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — CoM2 1.05.11 `Caster.exe`; calculator — `com2_1.05.11` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F74 | Confirm class **B** for `mom_1.31` and `mom_cp_1.60.00` (59 presets) against the settled policy: each asserts only that nothing moves, so the question is whether that absence is the rule under test. Where it is, make the `desc` say so; where the preset is named for a positive rule and merely happens to be inert, it is a class **A** defect and gets re-aimed. Reproduce the list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`; calculator — `mom_1.31`, `mom_cp_1.60.00` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F75 | Confirm class **B** for `com2_warlord_1.5.12.7`, `com2_1.05.11` and `com_6.08` (45 presets); same question as [F74](#confirmed-and-suspected-defects). Includes `landLinkingRangedCoM2`, half of the one structural finding: the version-difference subgroup `{landLinkingRangedCoM, landLinkingRangedCoM2}` expects the same 0/2 from both members, so it demonstrates nothing about versions — yet `landLinking` measures 3 against 1 on a fantastic unit’s melee, so the pair is re-aimable rather than dead. Versions: evidence/builds — Warlord 1.5.12.7 scripts, CoM2 1.05.11 `Caster.exe`, CoM 6.08 `WIZARDS.EXE`; calculator — `com2_warlord_1.5.12.7`, `com2_1.05.11`, `com_6.08` (test coverage only, no behavior change). | medium | [F61](#confirmed-and-suspected-defects) |
| F76 | Triage class **D** (37 presets, all five versions) — negative claims where some but not all named features are inert. The live half means the preset measures something; the question is whether the inert half is the exclusion being claimed or dead weight in the fixture. For each preset: read it against the rule its key claims, then either re-aim it onto a fixture where that rule is observable, restate it as an explicit negative claim if the absence is the real subject, or delete it. Reproduce the class list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — not applicable; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com2_1.05.11`, `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F77 | Triage class **E** (43 presets, 32 of them Warlord) — the key names no feature the preset configures, so the sweep cannot bind a claim to it and reports nothing either way. Some are token mismatches where the effect and its control have different names — the whole Goblin Pox family keys on `goblinPox` while the control is the global `poxHost` — and some are presets whose key names an effect they do not actually configure. Decide per preset between renaming the key, giving the sweep an alias map, and fixing the fixture. Reproduce the list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — not applicable; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08`, `com2_warlord_1.5.12.7` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F78 | Triage class **F** (25 presets) — the preset configures nothing the sweep can ablate. Most are baseline and roster scenarios where that is correct and expected (`rangedMissileBasic`, `thrownBasic`, `fireBreathBasic`, the `distPenalty*` set, the `predef*` roster matchups): those need confirming, not fixing. The rest need reading for whether the scenario states any rule at all. Reproduce the list with `node tools/preset_vacuity_sweep.js`. Versions: evidence/builds — not applicable; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08`, `com2_1.05.11` (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F79 | Run the sweep’s `--interactions` pass over the suite — it has never been run — and read the 26 ordering-named presets against it. The pass computes `both — onlyA — onlyB + neither` for each feature pair; a zero term means no ordering between the two can move the number, so an ordering-named preset carrying one is not testing the order it claims. 23 of the 26 sit inside the pass’s 2‑to‑4-feature window and 23 are clean under every other class, so this is the only check that reaches them. A zero at one operating point is a finding, not a proof of disjoint write sets: re-size the fixture before concluding. Versions: evidence/builds — not applicable; calculator — all five (test coverage only, no behavior change). | small | [F61](#confirmed-and-suspected-defects) |
| F92 | Perform [M11](./HISTORY.md)'s deferred `flameBlade` c-pair merge. **The region-`b` question it waited on is closed: the step stays at `c:flameBlade:ranged`, `SCOPE_ALL`, and no version split is needed.** Warlord runs CoM2's `Caster.exe`, whose Flame Blade block is `$0059FE47..$005A00E7` — after Focus Magic at `$0059A63B..$0059ABF7` — and gates its missile write on the **current** `U.rangedtype`, which Focus Magic has by then set to 34. `EncFlameBlade` occurs exactly once in `UnitCalcPre.CAS` (`:840`, Fiery Fury setting the flag) and once in `UnitCalc.CAS` (`:331`, the +1 fire breath `d:flameBlade` already carries), and nowhere else in the 8 files that name it: no Warlord script makes a blade ranged-strength write at all, so `UnitCalcPre.CAS:840-845` is Fiery Fury's own write and `b:fieryFury` carries it. The position does move numbers, in Warlord alone and only under Focus Magic — measured at region `b` against the shipped `c`, a 4-strength missile finishes 6 rather than 4, and a 3-strength Thrown that Focus Magic moves into an empty Ranged field finishes 5 rather than 3; the other four versions are unmoved. The `c` result is the engine's and equals `com2_1.05.11`, which is what [F83](./HISTORY.md) recorded. **The merge itself now blocks on [F111](#confirmed-and-suspected-defects):** `c:flameBlade:ranged` also carries Metal Fires' ranged +1, so merging it into `c:flameBlade` as it stands would file a MoM-only enchantment's write under the player-facing Flame Blade id, which is what M11 exists to end. Versions: evidence/builds — Warlord 1.5.12.7 scripts, CoM2 1.05.11 `Caster.exe`; calculator — all five, no arithmetic change once F111 has landed. | small | **Blocked on F111.** [predicate inventory](../Reference%20docs/Attack-type%20predicate%20inventory.md) E72, which now records the closed position question; [F83](./HISTORY.md), [F84](./HISTORY.md), [M11](./HISTORY.md); `PROVENANCE[flameBlade:ranged]` |
| F113 | **Audit the whole codebase for fallbacks that replace an out-of-range value instead of stopping.** `SPEC.md`, *Out-of-range values stop the run*, is the rule; this is the sweep that brings the existing code under it. [F112](./HISTORY.md) is the worked case and shows the shape to hunt: a lookup whose miss-path invents a plausible member of the target set. Cover every layer — the INI and roster parsers and generators under `tools/`, id-to-class and name-to-token maps, version dispatch, `PRESETS`/`TEST_TREE` fixture reads, and `Calculator/*.js` reads of enumerated fields. Search at least `\.get\([^,]+,` and `getattr\(.*,.*,` in Python; `\|\|` and `??` defaulting on a lookup result, `\[x\] \|\| DEFAULT`, and `switch` `default:` arms that return a value rather than throw, in JS. **Classify, do not blanket-replace:** a default a cited source defines is transcription and stays (`Hit=30`), a default the code invented is a defect and must raise, and a case the sources leave undefined is a new backlog row rather than a value. Each converted site names the value, its origin record or file, and the expected set in the error. Expect the sweep to surface unmapped enumeration members that no shipped data reaches today — those still convert, because the point is the next roster or mod, not the current one. Report a count by class so the audit is checkable, and add a check that fails on a reintroduced silent fallback if one can be bound. Versions: evidence/builds — not applicable; calculator — `all` must be verified unchanged except where a converted site proves a live misclassification, which is a finding to report before fixing. | large | [SPEC.md](./SPEC.md), *Out-of-range values stop the run*; [F112](./HISTORY.md) as the worked example |
| F111 | **Metal Fires' ranged +1 sits at the Flame Blade position and in three versions Metal Fires does not exist in.** `c:flameBlade:ranged` (`stats.js`) applies `fbAtkBonus` for Metal Fires as well as for the blades, under `SCOPE_ALL` and at the Flame Blade block's chain position, while Metal Fires' own melee write is `c:metalFires`, `SCOPE_MOM`, much later in the MoM chains. `unitcalc.c` 131:0x9065F is one Metal Fires block holding both writes — melee at 0x906C1, ranged at 0x906FC — far after Flame Blade's at 0x8F56E. Two measured consequences. The **position** is inert in MoM: moving the step to the `c:metalFires` slot produced 0 differences over 3,240 `metalFires` derivations in `mom_1.31` and `mom_cp_1.60.00`, crossing six ranged types, three strengths, nine ability sets and five environments. The **scope** is not: a `metalFires` input in `com_6.08`, `com2_1.05.11` and `com2_warlord_1.5.12.7` takes the +1 on Ranged and Thrown with **no** melee, because the `SCOPE_MOM` melee step is filtered out where the `SCOPE_ALL` ranged branch is not — a MoM-only enchantment half-applied in three non-MoM versions, and the modifier trace names `flameBlade:ranged` as its source. `ui_abilities.js` hides the control outside MoM, so `deriveUnitStats` is the only path that reaches it. Correcting it moves numbers in three versions F92 declares must stay unchanged, which is why it is filed rather than folded. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`; calculator — `com_6.08`, `com2_1.05.11` and `com2_warlord_1.5.12.7` change, `mom_1.31` and `mom_cp_1.60.00` verified unchanged. | small | Measured 2026-08-21 while settling F92; `unitcalc.c` 131:0x9065F-0x9072B against 131:0x8F56E; blocks F92 |
| F93 | Decide whether the magical-ranged classification should be data-driven. The engine reads `Ismagic` per entry from a moddable table (`Units.RecalculateUnits.pas:2968-2975`); the calculator classifies through the six fixed tokens of `RANGED_TYPES` (`data.js:37`), which roster generation assigns from the numeric id. **Measured 2026-08-21; the row's cost premise is falsified and the decision is unchanged.** Redirecting `isMagicalRangedType` (`combat_abilities.js:166-169`) fixes nothing: it names four of the six tokens (`magic_c`, `magic_n`, `magic_s`, `beam`), reads them faithfully, and the id is already discarded before it runs. The lossy step is the id→token map, now one per-version home in `tools/ranged_types.py` that raises on an id its table does not define ([F112](./HISTORY.md)). Table counts re-confirmed: CoM2 1.05.11 base 14 sections / 9 `IsMagic` / 2 `IsMissile`; Warlord 1.5.12.7 19 / 10 / 3. **Coverage.** The live maps cover ids 10, 11, 20, 21, 30–38, 40 and reproduce `IsMagic` exactly on every one: all nine (CoM2) / ten (Warlord) `IsMagic` ids land on `magic_c`/`magic_n`/`magic_s`/`beam`, and no non-`IsMagic` id does. Uncovered: Warlord-only 12, 13, 14, 22, plus reserved 39. **Reachability.** Exhaustive over both shipped rosters — 201 CoM2 records / 78 `RangedType` keys and 364 Warlord / 156, counted over every occurrence of the key, with no section shape other than `[N]`. CoM2 uses covered ids only. Warlord **reaches four uncovered ids on seven units with positive Ranged**: `[184]` Stone Giant, `[185]` Colossus, `[260]` Lesser Gaia Lord and `[261]` Great Gaia Lord (id 12), `[361]` Goblin Midget Submarine (14), `[246]` Arquebusiers and `[247]` Musketeers (22). All seven fall to the generators' `'Missile'` default. That default is **right for `IsMagic`** on all seven — none is `IsMagic` — so no magical answer in either shipped roster is wrong because of the fixed tokens; the one wrong magical answer that does exist, `[362]` Wanderer, is [F107](#confirmed-and-suspected-defects)'s generator gap, not this. It is **wrong for the sibling `Ismissileranged`** on five of them, which is split out as [F112](./HISTORY.md) and owns that finding. Ids 39 and 40 are unused by both rosters; `beam` reaches the record only through `base:energyCannon`, which is `isWarlord`-gated (`stats.js:780`) and so only ever meets Warlord's `[40] IsMagic=Yes`. The engine's level ladder splits two ways on `Ismagicalranged` with everything else falling to `NormalMissileRanged` (`Units.RecalculateUnits.pas:547-555`), so the boulder class costs E29 nothing. **Two defensible answers; the evidence settles neither.** (a) **Load the table** — carry per-version `{id: {isMagic, isMissile}}`, emit the numeric id from both generators, and classify at the read sites. The id-12/14 misclassification it would also have fixed is split out as [F112](./HISTORY.md) and no longer rides on this decision; what remains unique to (a) is support for modded entries up to the tables' stated id 100, and it matches the engine's shape (Focus Magic writes `U.rangedtype := 34` literally). Its cost is **large, not medium**: the token vocabulary is also the Custom-unit control's value set, the stat-card icon selector and a literal in several hundred presets' `rtbType`, and the DOS versions have no such table, so both shapes would have to coexist. (b) **Record a bounded deviation in `SPEC.md`** — the calculator classifies ids into six tokens at roster-generation time instead of reading `IsMagic`/`IsMissile` at runtime, bounded by the measurement above: exact for `IsMagic` on every id either shipped roster reaches and on every id the calculator writes at runtime, with modded entries out of scope. Cheap, but the bound is “correct for the magic byte” rather than “the table is unreachable”, and it leaves the id-12/14 defect standing. **A third path, offered for the decision rather than taken:** the map is already id-keyed, so completing it (12/13/14→`boulder`, 22→`missile`) and giving it one home fixes the reachable defect at small cost without the architectural change, after which (b)'s deviation is about runtime modding alone. Not folded in here — it corrects the sibling predicate, needs its own Warlord preset and a roster regeneration, and F93 is a decision item. Versions: evidence/builds — CoM2 1.05.11 `Caster.exe`, CoM2 1.05.11 base scripts, Warlord 1.5.12.7 scripts, both shipped `UNITS.INI` rosters; calculator — `com2_1.05.11`, `com2_warlord_1.5.12.7`. | small (b) / large (a) | split from [F89](./HISTORY.md); [predicate inventory](../Reference%20docs/Attack-type%20predicate%20inventory.md) E1, E49, E52, and E29 for the same shape in `Levelbonus.INI`; [F84](./HISTORY.md); [RangedType.INI](../Reference%20docs/Script%20source/Warlord%201.5.12.7/RangedType.INI) and its [CoM2 base counterpart](../Reference%20docs/Script%20source/CoM2%201.05.11%20base/RangedType.INI) |
| F109 | Take the DOS material body's fabricated `calcBaseRtb > 0` off both its arms, or record why it stays. `quality > 0`'s secondary group is one type-only gate — `RAT_CLASS(bu->ranged_type) == RAT_CLASS_MISSILE || RAT_CLASS_BOULDER || bu->ranged_type == RAT_THROWN` (`unitcalc.c`, 131:0x8F089/0x8F09C/0x8F0A4, 160:0x8F09B/0x8F0AE/0x8F0B6, com1:0x8F070/0x8F083/0x8F08B) — and the three writes inside it, `bu->ranged += quality - 1`, `Gold_Ranged` and `ranged_tohit++`, make no strength test, exactly as `ApplyMagicWeapons`' ranged arm makes none. `c:weapon` (`stats.js`, `weaponStatSteps`) still holds both DOS arms behind `context.calcBaseRtb > 0`, the slot's *input* strength — the last of the precomputed-modifier gates [M14](./HISTORY.md) retired elsewhere, and the same fabrication [F95](./HISTORY.md) removed from the level ladder's modern arm and left on its DOS one. Filed rather than folded into [F97](./HISTORY.md) because removing it **moves numbers in all three DOS versions**, outside that item's declared scope: over a 22,528-case weapon-material sweep, 164 `mom_1.31`, 162 `mom_cp_1.60.00` and 132 `com_6.08` cases move, every one a shared slot carrying a Missile, Boulder or Thrown *type* at zero input strength which then takes the material's `quality - 1` and its `+10` threshold — the DOS twin of the Warlord `[362]` Wanderer record F95 found. Establish whether a DOS template can carry that pair, and whether the Custom card should reach it. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`; calculator — `mom_1.31`, `mom_cp_1.60.00` and `com_6.08` may change, `com2_1.05.11` and `com2_warlord_1.5.12.7` must be verified unchanged. | small | filed 2026-08-21 from the [F97](./HISTORY.md) measurement; `PROVENANCE[weapon]`, `PROVENANCE[weapon:toHit]`; [DOS reconstructed](../Reference%20docs/DOS%20reconstructed/README.md) `unitcalc.c` 131:0x8F02A |
| F106 | Decide whether MoM 1.31's second `BU_Apply_Specials` call needs a chain position of its own. The routine has two callers — the battle-unit constructor (131:0x8F2A2) and the stat recompute (131:0x90A1D, immediately before Warp Creature at 0x90A2E). The recompute call filters its enchantment argument to what the battle unit gained during combat, but **1.31 alone passes the mutations byte whole at both sites** (CP 1.60 and CoM 1 zero it), so its three Chaos Channels blocks run twice. The chain models one position, the constructor's: `c:chaosChannels:armor` carries the doubled `+6` there ([M7](./HISTORY.md)), and [F103](./HISTORY.md) put `c:chaosChannels:fireBreath` at 0x8F720 in the same run. Folding is exact for the additive armor bonus and lossy for the breath, which **assigns** the shared slot: the engine's second pass would re-assign it to 2 after the recompute's own writes (node aura, Prayer, Darkness, Metal Fires), and the calculator's single pass leaves those in place. Establish which recompute writes actually reach the shared slot ahead of 0x90A1D, then either add the second position to the 1.31 chain or record the fold as a deliberate deviation in `SPEC.md`. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`; calculator — `mom_1.31` may change, `mom_cp_1.60.00`, `com_6.08`, `com2_1.05.11` and `com2_warlord_1.5.12.7` must be verified unchanged. | small | `MoM binary analysis.md`, *`BU_Apply_Specials` runs twice*; `PROVENANCE[chaosChannels:armor]`, `PROVENANCE[chaosChannels:fireBreath]` |
| F60 | Revisit the DOS per-figure Doom Gaze scaling. The DOS engines deliver type-104 Automatic Damage inside `BU_ProcessAttack`'s per-attacker-figure loop, so Doom damage scales with the gazer's figure count; CoM2 and Warlord pass a literal figure count of `1` and deal it once. The calculator models the unscaled form for every version — `combat_special_attacks.js:183-187` adds `doomStr` once regardless of `atkAlive` — which is correct for CoM2/Warlord and a deliberate divergence for the DOS builds. No predefined roster reaches it: the only `Gaze(Multiple)` unit in `units_mom.js` and `units_com.js` is Chaos Spawn at one figure. Determine (1) whether a Custom DOS unit can select Multiple Gaze with more than one figure and so reach the divergence through the UI, and (2) whether CoM 6.08 shares the MoM builds' per-figure delivery — the gaze dispatch sites are byte-identical across the three builds, but the evidence asserts the scaling for MoM only. If reachable, model the scaling for the DOS versions with a preset proving the result changes with figure count; if unreachable, record it as an accepted limitation instead. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`, CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 `Caster.exe`; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08` (behavior may change), `com2_1.05.11`, `com2_warlord_1.5.12.7` (unchanged, already correct). | small | `MoM binary analysis.md`, *Gaze attacks* (Automatic Damage OR at `0x99B55`, rolls bypassed `0x9A1E6`→`0x9A204`, loop bound `0x9A579`); `CoM2 binary - combat flow.md`, *Gaze attacks* (literal `1` at `0x5B3858`/`0x5B3982`) |
| F41 | Add engine-specific hero progression: the DOS eight-threshold ladders and template-ability formulas (including CoM's Blademaster divisors, mana table and Lucky rewrite), plus the modern nine-step hero table. Decide how the current six-entry level control exposes them. | large | R6.1f evidence, hero ladders and abilities; CoM2 tables, level bonuses; `combat_abilities.js:43-78` |
| F107 | Source the Wanderer's permanent ranged type from the roster, and take the Marionette realm retype off the permanent record. `UNITS.INI [362]` gives the Warlord Wanderer `RangedType=30` with `Ranged=0` — the roster's only record whose ranged type names an attack at zero strength, and the one [F95](./HISTORY.md)'s ranged gate now reads. `tools/generate_warlord_units_json.py` drops the field when `Ranged` is zero, so `Calculator/units_warlord.js` entry 362 carries no `ranged_type` and the calculator recovers the fact only through `deriveMarionettePackage` (`stats_identity.js`), which hardcodes `rangedType: 'magic_c'` for the strayed branch and a per-realm map for the owned one. The owned map is on the wrong record: `SETSTAT(U,SRangedType,0,...)` (`UnitCalcPre.CAS:104`, `:122`, `:140`, `:158`, `:176`, `:272`) is record selector `0`, a region-`b` calculated write, but `baseSequenceRangedType` (`stats.js`) seeds it into what `base:stat:base` calls the permanent record, where every `base`- and `a`-phase reader and now `c:level` can see it. **Inert today:** RangedType 30 and all five realm types are `IsMagic=Yes` in `RangedType.INI`, so the level ladder picks `MagicRanged` either way, and the two columns are column-identical in the shipped `Levelbonus.INI`. The strayed branch writes no ranged type at all (`UnitCalcPre.CAS:364-392`), so whether the calculator should project one there is the second question. Fix the generator first: with `ranged_type` present on entry 362 the permanent fact has a roster home and the Marionette package only has to carry the region-`b` retype. Versions: evidence/builds — Warlord 1.5.12.7 `UNITS.INI`, Warlord 1.5.12.7 scripts, CoM2 1.05.11 `Caster.exe`; calculator — `com2_warlord_1.5.12.7` may change, `com2_1.05.11`, `mom_1.31`, `mom_cp_1.60.00` and `com_6.08` must be verified unchanged. | small | filed from the [F95](./HISTORY.md) measurement, which found entry 362 the one roster record its ranged gate moves; `PROVENANCE[marionettePackage]`; [predicate inventory](../Reference%20docs/Attack-type%20predicate%20inventory.md), *Not predicates*, which listed the Marionette type assignments without asking which record they sit on |
| F108 | Settle CoM 1 Focus Magic's ammunition gate and the breath/gaze reach of its two `+3` arms. CoM 1 runs the block only `if (bu->ammo > 0)` and then takes a three-way branch: `base_rt >= RAT_MAGIC_FIRST && base_rt != RAT_THROWN` on the **unit type's** ranged type, else `bu->ranged_type > RAT_THROWN` on the live one, else retype to 34 with a minimum strength of 3 (`unitcalc.c`, com1:0x8F7E6-0x8F84C). Two gaps. **The ammo gate is unmodelled:** ammunition is a stated non-goal ([SPEC.md](./SPEC.md), *Deliberate deviations*), so `c:focusMagic` runs unconditionally and a CoM 1 unit with no secondary attack gains a strength-3 `magic_s` attack the engine's gate may withhold — the same shape as the Warlord Energy Cannon inference, and it needs the same explicit decision rather than a silent default. **Both `+3` arms reach past magical ranged:** 101-105 are the two breaths and the three gazes, so the first arm admits a permanent breath *or gaze* template and the second admits a live one, while the calculator's CoM 1 body admits live magical ranged and the two breath types only. A CoM 1 type-104 unit therefore takes no `+3` on the shared value that carries its Doom damage, which is what `focusMagicDoomGazeCoM` currently pins at 2.000 as the paired negative of `focusMagicDoomGazeCoM2`; if the second arm applies, that expectation is 5.000 and the pair's claim has to be restated. Filed rather than folded into [F98](./HISTORY.md) because the ammo half is a modelling decision, not a cited correction, and because the gaze half moves an existing preset's expected number. Versions: evidence/builds — CoM 6.08 `WIZARDS.EXE`; calculator — `com_6.08` may change, `mom_1.31`, `mom_cp_1.60.00`, `com2_1.05.11` and `com2_warlord_1.5.12.7` must be verified unchanged (`c:focusMagic` is `SCOPE_COM_PLUS`). | small | `PROVENANCE[focusMagic]`; [DOS reconstructed](../Reference%20docs/DOS%20reconstructed/README.md) `unitcalc.c` com1:0x8F7E6; filed 2026-08-21 from the [F98](./HISTORY.md) implementation |

## Modelling work

Accepted limitations remain in `SPEC.md`; only planned changes appear here.

| ID | Upcoming task |
|---|---|
| M3 | **Blocked by F41:** implement the older-engine Destruction path, including elemental protection. Present in all three DOS builds but hero-only, so it stays unreachable until heroes are modelled; 1.31 adds the defender's per-figure hits where CoM 1 adds a flat 100. |

## Context cost and maintainability

T8–T11 are documentation- and naming-quality items filed here for want of a better home.

| ID | Upcoming task | Cost |
|---|---|---|
| T8 | **Low priority.** Give the code's unsourced engine claims either a citation or an evidence home. [T6](./HISTORY.md) measured the gap: of 319 multi-line prose blocks in the twelve formula-bearing sources, **227 (97 KB) assert engine behavior with no `PROVENANCE`/`STAT-FORMULA` citation within eight lines**, so `npm run provenance` cannot see them and nothing checks them. Some are already correct and sourced elsewhere — `stats.js`'s Weakness block cites `0x908FC`/`0x9067F` and the unsatisfiable-`int8` argument that `MoM binary analysis.md`, *Weakness* records, but its nearest citation is 19 lines away and belongs to `rust:ranged`. Others may have no evidence at all: `stats.js`'s Military Workshop block attributes magnitudes to Warlord patch numbers (1.5.7.4, 1.5.9.5) that no evidence document mentions. Triage each block into (1) claim already in evidence — add the pointer, (2) claim checkable but uncited — cite it, (3) claim unsupported — open a question. This is an evidence-coverage question, not a context-cost one; T6's boundary decides only what may be removed once a home exists, and the two smaller cleanups T6 left in place belong here: the Military Workshop block restates its own opening lines, and `combat_state.js`'s ability catalogue drifts unchecked. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`, CoM 6.08 `WIZARDS.EXE`, CoM2 1.05.11 `Caster.exe`, Warlord 1.5.12.7 `Caster.exe`; calculator — all five, comments only, no behavior change. | large |
| T9 | **Low priority.** Rename the `legacy*` identifiers that mean "the DOS versions". DOS support is permanent, so the word wrongly implies deprecation. Footprint: ~100 lines in `Calculator/` over 14 identifiers — `legacyThrown`, `legacyLifeStealBenefit`, `legacyUnitTypeFromIdentity`, `legacyClamp`, `legacyLightDark`, `legacyBaseRace`, `legacyType`, `legacyApply`, `legacyDefenderGazeLabels`, `legacyAttackerGazeLabels` and four others — plus 12 lines in `Calculator/*.md`. Choose one replacement term (`dos*` is the obvious candidate) and apply it to code and docs in one mechanical pass; this does not touch [M7](./HISTORY.md)'s unrelated "legacy callers" sense of `unitType` as a compatibility projection. Versions: evidence/builds — not applicable; calculator — `mom_1.31`, `mom_cp_1.60.00`, `com_6.08` (naming only, no behavior change in any version). | small |
| T10 | **Low priority.** Finish the step-id residue [M13](./HISTORY.md) left. Two parts, both naming/trace identity only. **(a)** `base:baseMelee`, `base:baseRtb` and `base:baseBlock` are chain-adjacent, share one `PROVENANCE` citation and are one engine block split by field — the shape M11 merges — but each carries a distinct user-facing source label (`Base melee To Hit`, `Base ranged/Thrown/Breath To Hit`, `Base To Block`), so merging them would put one label on three modifier-trace rows. Decide whether a merged step may carry a per-field label, or whether three labels are reason enough to keep three steps, and say which in `SPEC.md`. **(b)** `d:trueSight:ranged` and `base:survivalInstinctToBlock` still name a field in the id with no sibling to distinguish, which `writes` already states; M13 stripped only the `chance:` prefix and left these. Versions: evidence/builds — not applicable; calculator — all five (naming and trace identity only, no arithmetic change). | small |
| T11 | **Low priority.** Drop the Chaos Surge carve-out that [F103](./HISTORY.md) made inert. `stats_sequence.js`'s `c:chaosSurge` skips a slot matching `c.ccFireBreathGranted && c.ccDosBreathEligible && version.startsWith('mom')` — a compensation for the fire-breath assignment then sitting in region `a`, ahead of Chaos Surge. With the write at its own address (131:0x8F720, after the constructor's Chaos Surge block at 0x8F113) the assignment produces the exclusion by itself, and removing the condition was measured to move no number in any of the five versions. Left in place because nothing observable distinguishes the two forms, so no preset can hold the removal. Versions: evidence/builds — MoM 1.31 `WIZARDS.EXE`, MoM CP 1.60 `WIZARDS.EXE`; calculator — all five, dead-condition removal, no behavior change. | free |

## Open questions

| ID | Question / next action | Evidence / blocker |
|---|---|---|
| Q1 | Resolve Troll Shaman/Magician roster values versus the manual, including the possible Troll +1 melee rule. | roster/manual conflict |
| Q2 | Confirm the apparent Draconian common-unit +1 Resistance racial modifier. | roster/manual conflict |
| Q5 | **Blocked:** identify the source and page behind “weird defense behavior on page 25.” | Missing source |
| Q6 | Resolve CoM High Prayer's +3-attack text versus the +2 used elsewhere. | prose conflict |
| Q8 | Determine whether Wraiths use Life Steal −4 or −3. | observation/manual conflict |
| Q12 | Determine when “ranged” includes missile, boulder, magical, Thrown, breath, or gaze across versions/effects. | Asked; awaiting answer |
| Q15 | Check whether compiled code ever consumes `unitT.savemodifier`. | CoM2 units, `unitT` fields; R4 |
| Q17 | Resolve CoM 1 FocusMagic's `ammo > 0` gate against helptext that promises the ranged attack unconditionally; the answer also decides whether the signed `base_rt` fallback is reachable in play. `Reference docs/DOS reconstructed/unitcalc.c:1117-1134` gates CoM 1's entire Focus Magic attack branch on `if (bu->ammo > 0)` (`com1:0x8F7FB`), with only `mana_max += 15` outside it, while the calculator applies `focusMagicCreatesRanged` to `com_6.08` with no equivalent gate (`stats.js:413-419`). Proposed resolution: check the **roster** for whether the inference used for Warlord's Energy Cannon ([SPEC.md](./SPEC.md):411-414, inferred from positive permanent conventional Ranged) applies here. No preset now depends on the answer: [F61](#confirmed-and-suspected-defects) re-aimed `focusMagicDoomGazeCoM`, whose 0.900 was the created ranged attack, onto the gaze it names. | R6.1a evidence, *Open questions*; `DOS reconstructed/unitcalc.c:1117-1134` |
| Q18 | Determine what CoM 1's unit-type ceiling of `0x97` selects, given that the helptext says "normal unit" but the ceiling appears to admit four fantastic summons. Two blocks read it: the Holy Arms grant (`com1:0x8F16C`) and Heavenly Light's weapon-quality test (`com1:0x905E7`), where it forces `cl` to Magic Weapons and so withholds the To Hit thresholds. [F49](./HISTORY.md) implements the weapon-quality condition and leaves the ceiling unmodelled pending this answer. | R6.1a evidence, *Open questions*; R6.1c evidence, *The CoM 1 city-defense bytes are Guardian and Heavenly Light* |
| Q19 | Resolve CoM 1 Realm Ward helptext's −2 To Hit/−4 Defense/−4 Resistance against the executed `0x90A87..0x90A9B` writes of −2/−3/−3. | R6.1d evidence, *CoM 1 Realm Wards use −2/−3/−3* |
| Q20 | Identify the CoM 1 `Move_Flags 0x0100` bit set by Supreme Light and verify whether its read path implements the helptext's post-combat regeneration. | R6.1d evidence, *CoM 1 Supreme Light has five eligibility paths* |
| Q21 | Resolve CoM 1 helptext's retained Endurance/Giant Strength powers against the manual's Teleportation/Inner Fire/Divine Protection replacements; the executed binary effects match the manual's replacement package. | prose conflict; R6.1e evidence, *Later builds turn the item block into a patch surface* |
| Q23 | Resolve CoM Logistics scaling: shipped helptext says +0.5 movement per two experience levels, while the manual says +0.5 per level. `Battle_Unit_Moves2` consumes the per-controller maximum at `DS:0x3AC8` but does not calculate it. | R6.1g evidence, *CoM `Battle_Unit_Moves2`*; prose conflict |
| Q25 | Resolve the R6.1h shared-source rendering disagreement at `0x8E5C6: 25 00 01`. Claude: the statement-level `#if` is “a naming branch only” and may use `IP_COM1_DIVINE_PROTECTION` versus `IP_POWER_DRAIN`. Codex: a statement-level `#if` “visually asserts a source/control-flow divergence” absent from the identical bytes, so one statement plus a build-selected alias is preferable. The merged C uses the alias form and the mechanic is undisputed. | R6.1h evidence, *Disputed source representation* |
| Q27 | Re-establish the CoM2/Warlord classification of Righteousness. It sits in the attack-specific defense transform's immunity-replacement slot "for compatibility" pending a decision that `SPEC.md` cited as open under `D1/D3` — IDs that exist nowhere in the repo, so the question was lost rather than resolved. Determine whether Righteousness belongs in that replacement slot for `com2_1.05.11`/`com2_warlord_1.5.12.7`, or is MoM-only as the resistance path treats it. | Orphaned reference found during the 2026-08-17 `SPEC.md` extraction; `combat_special_attacks.js`, attack-specific defense steps |
| Q26 | Resolve the R9-G1a-R3 evidence-scope disagreement. Codex: “Retain only raw `[0x2AED2,0x2AED4)` (`81 00`) for the target value” and cite checked-in mapping/name evidence. Claude: “Restricting the read to the two target bytes is exactly the position that produced the wrong record base”, so the target's neighbouring name-pointer rows, table origin, ability-mask bounds and selected strings are the minimum locate evidence. The base, target address/value and constructor linkage are undisputed. | R9-G1a-R3 evidence, *Disputed evidence scope*; data D1–D8 |

## Blocked on people

| ID | Needed answer |
|---|---|
| H1 | **Human:** Contact Seravy about the confirmed CoM2/Warlord Blur bug: turn-relative `CGADEnemy` makes the opposing army's Blur reduce that army's own retaliation gazes and counterattack. Record whether this is intended and whether a future engine build should change it. |
