
Document: CLAUDE.md
Section: new section after "Protocol for execution of TASK items"
Change: addition
Text:
## Version control

Work happens on `main`. There is no branching model.

Commits are not per TASKS item: one commit bundles roughly 5–10 finished subtasks, fewer when the
changes are large. The per-item commits in the history are individual agents' choices, not a
convention to continue.
