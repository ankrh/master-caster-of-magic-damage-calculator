# Project instruction entry point

Before doing any work, read `CLAUDE.md` in this directory in full unless already in
context. It is the shared project contract for Claude and Codex and identifies the
shared global instructions, memory and task execution procedures. Follow those
rules; do not duplicate them here. If the file cannot be read, report the missing path
or access error explicitly rather than assuming its instructions were loaded.
This entry point is CONTRACT tier.
